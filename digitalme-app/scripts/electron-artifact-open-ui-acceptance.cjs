"use strict";

/**
 * RETIRED as formal / UI click evidence (ARTIFACT-ACCESS-MIN-01).
 * Artifact access is via native File menu only until a unified command bus exists.
 */
console.error(
  "RETIRED: card-button open UI harness is not valid evidence.\n" +
    "Use npm start → 文件 → 打开当前成果 / 打开成果所在文件夹."
);
process.exit(2);
