"use strict";

/**
 * RETIRED as formal evidence (ARTIFACT-OPEN-RESET-01).
 * Coordinate-click / isolated-session harnesses must NOT be used to claim
 * formal_electron_click_passed or owner availability.
 *
 * Use: npm start + real mouse on Owner userData.
 */
console.error(
  "RETIRED: electron-owner-artifact-open-single-entry.cjs is not formal evidence.\n" +
    "Run: cd digitalme-app && set DIGITALME_ARTIFACT_OPEN_DIAGNOSTIC=1 && npm start\n" +
    "Then click「打开成果」with a real mouse."
);
process.exit(2);
