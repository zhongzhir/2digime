# Digital Me 人生轨迹与社会全貌（信息结构 v0.1）

日期：2026-07-11（2026-07-12 修订：关系口径、时间轴编辑、「我」栏目）  
状态：结构原则（指导后续 Package schema；工程分期）  
触发：任职/履历类材料不应被当成「表格字段抄录」或「人格蒸馏」，而应沉淀**带时间的社会事实**（角色、机构、关系、结果）。  
关联：`digitalme_material_guidance_v0.1.md`、`digitalme_person_definition_v0.1.md`、`digitalme_context.md` §2.5、决策 #25–#30

---

## 1. 核心判断

**全貌 ≠ 把每张表的格子再提取一遍。**  
**全貌 = 人在时间中的角色、关系、行为与结果，可核对、可增补、可授权使用。**

静态履历（出生地、早年学历等）**登记一次即可**；反复导入同类材料时，重心应是：

> 在什么时间段，以什么身份，与什么机构/人，发生了什么，留下什么结果。

例如：「任某公司某职」「加入某协会并担任理事」是社会角色事实；表上的标准履历字段不是每次导入的主产物。

产品入口：侧栏 **「我」** → 时间线为主交互；观念材料走「观念与表达」。

---

## 2. 推荐结构：时间主线 + 维度表

```text
                    ┌─ 角色任职 (roles)           ← 由事件派生
                    ├─ 关系自我 (relations)       ← ego–alter；机构不算关系
  人生事件主线 ──────┼─ 资产与项目权益 (assets)     ← 默认高敏
  (events / timeline)├─ 成绩与结果 (outcomes)
  【真时间轴可编辑】  ├─ 兴趣与偏好 (interests)
                    ├─ 能力与作品目录 (capabilities / works)
                    ├─ 地理与生活节律 (places / rhythm)  ← 可后置
                    └─ 约束与禁区 (boundaries)         ← 系统默认 + 用户确认改
```

### 2.1 人生事件主线（Spine）

每条事件尽量含：

| 字段 | 含义 |
|------|------|
| `when` | 时间点或区间（可模糊：年 / 年月） |
| `what` | 发生了什么（一句话） |
| `roles` / `roleLabels` | 当时的身份标签（董事、顾问、项目负责人…） |
| `actors` | 涉及的人 / 机构（机构用于角色，不自动变「关系」） |
| `outcome` | 结果或状态变化（可选） |
| `evidence` | 来源材料（简历、任职证明、新闻…） |
| `facets` | 回写到哪些维度表 |

事件是**增量事实的入口**；维度表是**当前可查询的切片**。  
**产品要求**：时间线必须是**以时间为轴**的可视化，并支持在轴上**添加 / 修改 / 删除**。

### 2.2 维度表

| 维度 | 记什么 | 示例 | 敏感度 | 产品状态 |
|------|--------|------|--------|----------|
| **角色任职** | 头衔、机构、任期、职责摘要 | 某职 @ 某机构 | 中 | ✅ 展示 |
| **社会关系** | **相对某人**的关系类型与不对称角色（非机构列表） | 同事、师徒、合作 | 中高 | ✅ 认知页：关系人候选 + 手动添加；机构走触点 |
| **机构触点** | 雇主/主办方/协会等（**非 L4**） | 某论坛主办方 | 中 | ✅ `org_touchpoints.json` |
| **资产与项目** | 持股、基金、项目参与身份（事实层） | 某项目参与人/投资人 | **高** | 后置 |
| **成绩与结果** | 里程碑、公开成果 | 出书、获奖 | 中 | ✅ 认知页 + outcomes |
| **议题与专长** | 领域信号 | 区块链、产业 | 低 | ✅ domains |
| **能力边界线索** | 擅长 / 不负责 / 范围 | 仅咨询不签约 | 中 | ✅ capability_signals |
| **开放推断** | 待证实、可调用 | 可能出席某活动 | 中 | ✅ inferences.jsonl；注入须标注非硬事实 |
| **兴趣爱好** | 稳定兴趣 | 某运动 | 低 | 可后置 Tab |
| **边界与禁区** | 永不注入 / 永不对外 | 系统默认清单 | 控制面 | ✅ 系统默认 |

### 2.3 关系（L4）正确口径

一条关系 = `me` + `alter`（自然人）+ `relationType` + `asymmetricRoles` + `context` + `when`。  
机构归属归角色表。无明确 alter 时**禁止**自动生成关系。详见 `digitalme_person_definition_v0.1.md` §3。

---

## 3. 与「三项导入」的关系

| 用户选项（v0.2.6 文案） | 写入哪里 |
|--------------------------|----------|
| 丰富表达与判断 | 表达/判断层（style、persona、frameworks、memory） |
| 登记人生事实 | **事件主线 + 角色等维度表**；落到「我 · 时间线」 |
| 仅保管 | 本地保险柜；可挂 `evidence` 指针，默认不进表达、不进对外代理 |

---

## 4. 任职材料示例（正确 vs 错误重心）

| 错误重心 | 正确重心 |
|----------|----------|
| 把表内学历、早期工作再提取成一长串「事实」 | 事件：`任 [机构] [职务]`（起止若有则写入） |
| 当成文风/价值观蒸馏 | 写入角色任职表；证据=该文件 |
| 每次导入都全量刷新履历 | 静态背景已有则跳过；只合并新角色/新任期 |
| 把机构当成「关系人」 | 机构 → 角色；人 → 关系（后者本轮不做） |

---

## 5. 数字化边界（重申）

1. **事件与维度表**服务「社会全貌与可追溯」；  
2. **人格蒸馏**服务「像我表达与判断」；  
3. **仅保管 / 禁区**保证「有≠会说」——禁区以**系统默认**为主，用户改动须确认。  

---

## 6. 工程分期建议

1. ✅ 导入分流 + 社会事实提示词；进度文案不用「蒸馏」。  
2. ✅ Package `life/` 事件与维度表雏形。  
3. ✅（2026-07-12）侧栏「我」；真时间轴增删改；错误关系 Tab 下线；禁区系统默认。  
4. **再后**：资产分表；ego–alter 关系 UI；观念层条目深编辑。

---

## 7. 一句话给产品

**用时间轴串起「我是谁、做过什么」；用角色表维护可查询的社会切片；用观念与表达维护可说话的人格；用系统默认禁区守住不该被代理的部分；关系等「相对他人」的定义成熟后再开。**

## 8. 文案约束

产品默认界面示例须用通用材料名与角色名（见产品规格 §2 第 10 条）。本文件中的举例仅供内部设计，写入 App 前须再做「陌生用户是否明白」自检。
