# Digital Me MVP 范围定义 v0.1

版本：v0.1  
状态：历史 MVP 基线；2026-07-16 起由第一阶段升级计划覆盖近期执行  
日期：2026-07-08

## 一、MVP 总目标

> **2026-07-16 审计后覆盖说明**：本文保留最初 MVP 范围与历史决策。当前代码已超过本文件中的部分功能切片，但安全、数据完整性与评测没有同步成熟。近期不继续扩展范围，执行 `digitalme_phase1_subject_upgrade_plan_v0.1.md`：主体可信化、已有能力硬化、主体产品感知和外部协作最小骨架。

将人与 AI 结合起来：**把人蒸馏进 Digital Me，形成明确属于自己的数字之我，并让它在数字世界中动起来。**

“动起来”包含三个循环：

| 循环 | 含义 | 结果 |
|---|---|---|
| 映射循环 | 持续收集人的数据（行为、成果、生理、情感等）沉淀到 Digital Me | Digital Me 成为人及人的工作生活的动态映射（数字孪生） |
| 应用循环 | 人主动应用 Digital Me 吸收信息、执行任务、产出产物 | Digital Me 成为人的能力放大器 |
| 反馈循环 | Digital Me 按预定规则对外界信息进行反馈调整 | Digital Me 成为受规则约束的相对独立数字存在 |

商业应用场景当前不清晰，**不作为 MVP 目标**；对外协作与交易仅做架构预留。

## 二、MVP 功能范围（In Scope）

### 1. 蒸馏：Digital Me Builder

1. 导入个人资料（文档、文章、对话记录、项目资料等）；
2. 生成并允许人工修正 persona、style-guide、preferences、decision-frameworks、memory、skill-index；
3. 输出符合 Package v0.1 结构的 Digital Me Package，支持版本化更新。

### 2. 映射循环：Data Ingestion Pipeline

1. 定义数据类别：行为数据、工作成果、交互记录，预留生理与情感数据接口；
2. 支持手动导入 + 定期采集（如指定文件夹、日历、笔记工具）两种方式起步；
3. 所有采集数据进入本地记忆库，按敏感度分层（本地专有 / 可云同步）；
4. 采集内容可查看、可删除、可标记为无效。

### 3. 应用循环：Runtime + 能力安装

1. 本地优先 Runtime，加载本人 Digital Me Package 作为运行上下文；
2. Model Gateway：可配置调用 OpenAI / Claude / DeepSeek / 本地模型；
3. Capability Installer：Digital Me 不自研任务功能，通过安装市场可用能力（Skill 文件、MCP 工具、插件）适应任务；
4. 任务执行时自动携带本人 persona、偏好与判断框架，产出“像我”的结果；
5. 每次任务执行写入调用日志（模型、能力、记忆引用、输出摘要）。

### 4. 反馈循环：Feedback Rules Engine

1. 支持定义反馈规则：触发条件（外界信息/事件）→ 允许的响应动作 → 是否需人工确认；
2. 低风险动作（如整理、归档、草拟、提醒）可按规则自动执行；
3. 高风险动作一律进入人工确认队列；
4. 用户修正（内容/风格/结论/风险/边界）回流更新 memory 与 policy。

## 三、暂不做（Out of Scope）

1. 对外协作调用与交易结算的实际功能（仅保留 Interaction Contract、commerce 数据结构预留）；
2. 区块链上链（仅保留 chain-anchor 可选字段）；
3. 大众社交、人格市场、多人网络；
4. 自动执行付款、签约、法律意见等高风险动作；
5. 机构数据授权交易（如"机构付费获得个人数据用于模型训练"）——理论可行但无先例、存在认知与习惯障碍，只做数据结构与授权机制上的条件预留；
6. **恋爱/Soulmate 匹配作为增长主路径**（2026-07-13）——与发展向主体定位冲突；关系导入若进入后续版本，须明示同意且动力为合作/价值创造。

## 四、MVP 验收标准（草案）

原验收标准仅证明“功能可运行”，不足以证明可信主体。第一阶段新增四个前置门槛：

1. 主体事实、本人声明、系统推断、当前状态和发展意图严格分层；
2. Package 有来源 hash、版本、原子写入、回滚、导出恢复；
3. 密钥受保护，高风险动作与数据出口由代码策略强制；
4. 用户能感知并操作能力名片、一次性协作授权和协作记录。

未通过以上门槛时，不得仅凭下面的原功能验收对外称为完整 Digital Me 产品。

1. 一个真实用户（创始人角色）能在 1 天内完成初次蒸馏，得到自己的 Digital Me Package；
2. 连续 2 周的日常数据能持续进入映射循环，记忆库可见增长且可管理；
3. 用户能通过安装至少 2 个市场能力完成 3 类以上真实任务（如研究、写作、决策辅助），产出被用户认可为"像我"；
4. 至少 3 条反馈规则可稳定运行，其中高风险动作 100% 进入人工确认；
5. 完整 Package 可导出并在另一环境（如 Claude Project / Cursor）降级使用。

## 五、已确认技术方向（2026-07-08，同日两次更新）

1. **首批数据源**：本地文件夹（文档、作品、项目资料）、聊天/对话记录、日历与邮件；笔记工具与可穿戴生理数据后期接入；
2. **能力安装路线**：MCP 优先，兼容 Skill 文件；
3. **反馈规则形式**：自然语言编写规则，系统编译为结构化 policy（保留 policy JSON 作为底层格式）；
4. **产品形态（更新）**：不再"先借 Agent 框架验证"，**直接开发独立的本地桌面应用**。理由：蒸馏层验证结果已有把握；产品核心部件（数据采集、规则引擎、审计、授权管理）在借用框架中本就无法实现；真实产品形态才能建立直观的产品认知。Cursor 的角色调整为开发工具；
5. **首版功能切片**：优先点亮"蒸馏 + 应用"循环（导入素材生成 Package，带"像我"引擎执行任务），映射循环与反馈循环随后分批点亮。架构一开始即保持完整骨架（身份、记忆、模型网关、审计），功能分期装入。

## 六、首版产品（Digital Me App v0.1）功能清单

1. **素材导入与蒸馏（Builder）**：接入 `source-materials/` 素材库，生成并可人工修正 Package 各文件，支持版本化；
2. **"像我"输出引擎**：蒸馏文件 + 素材向量索引（本地）+ 原文示例检索 + 修正回流四层组合；
3. **任务工作台**：对话式使用自己的 Digital Me 完成研究、写作、决策辅助等任务；
4. **能力安装**：作为 MCP 客户端安装外部能力；
5. **基础审计**：每次任务记录模型、能力、记忆引用与输出摘要；
6. **Package 导出**：Lite / Standard / Full 分层导出。

建议技术栈（开发启动时最终确认）：桌面壳（Tauri 或 Electron）+ TypeScript、本地 SQLite（含向量检索）、MCP TypeScript SDK、模型网关支持 OpenAI / Claude / DeepSeek / 本地模型。

## 七、下一步开发准备任务

1. 定义数据采集的目录约定与元数据格式（对齐 Package v0.1 的 memory/sources 结构）——素材库目录已建立；
2. 确定技术栈并搭建应用骨架（本地存储、模型网关、Package 读写）；
3. 实现 Builder 第一版：从 `source-materials/books/` 的著作完成首次蒸馏；
4. 实现"像我"引擎与任务工作台，以本人日常任务实测；
5. 设计"自然语言规则 → policy JSON"编译映射（反馈循环预研）。
