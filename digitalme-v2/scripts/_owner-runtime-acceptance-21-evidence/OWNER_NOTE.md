# 第三次真机验收（进行中）

软件已经打开。请像平时一样用，不要按脚本点。

可丢弃的小项目在：

`D:\Projects\DigitalMe-Software-UX-Owner-Test-1786603793016`

你可以自然走完这些事（顺序按你习惯，不必对照内部步骤）：

1. 让它帮你做一件事。例如：改这个项目里的 formatLabel，使输入 start 时返回 start-processing，并运行测试。不要提交或推送。
2. 做完后，用平常的话问问：现在能不能用、还要不要改、有什么需要你知道的。
3. 如果想改，直接说清楚。例如：把 start 的返回值改成 done，测试一起改。
4. 关掉软件后告诉我，我用同一入口再打开；看是不是还是刚才那件事。
5. 全部结束后，只回答三句：
   - 体验是否可信
   - 结果是否正确
   - 是否愿意采用

我不会替你点采用。遇到失败、看不懂、或觉得不对，停下来原样告诉我即可。
