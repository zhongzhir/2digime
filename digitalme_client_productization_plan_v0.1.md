# Digital Me 客户端产品化与多端演进计划

版本：v0.1  
日期：2026-07-23  
状态：`deferred` / **后续规划，不是当前执行计划**  
前置条件：现有功能继续完成数轮更新优化，并通过 Owner Electron 真机验收后再启动。

## 0. 文档定位

本文解决 Digital Me 从“终端命令启动的开发版”走向普通用户产品的问题，覆盖：

1. Windows 桌面安装与点击启动；
2. Web 轻客户端；
3. 手机端轻交互与正式 App；
4. 本地 Runtime、Package、云边服务和多端之间的边界。

本文不改变当前功能开发顺序，也不替代当前唯一执行计划。当前产品功能、验收和实现任务仍以 `digitalme_context.md` 指向的现行基线和计划为准。

## 1. 产品化目标

### 1.1 普通用户目标

用户应当能够：

> 下载 → 安装 → 点击桌面图标 → 首次配置 → 直接使用 Digital Me

用户不需要打开 PowerShell、记忆路径、执行 `npm install` 或输入 `npm start`。

### 1.2 产品化原则

- 应用程序与个人数据分离；
- 用户资料、Digital Me Package 和重要记录默认由用户掌握；
- 升级不能覆盖或破坏用户数据；
- 安装失败、模型配置失败和数据恢复失败必须用普通用户语言说明；
- 桌面端先承担重度工作，手机端先承担轻交互和重要确认；
- 多端扩展不能把完整个人主体明文集中到云端。

## 2. 当前基线与主要缺口

### 2.1 已有基础

- 当前应用已经使用 Electron；
- 已有 `npm start` 启动脚本；
- 已有主进程、预加载层、渲染界面和本地 `userData` 配置路径；
- 已有 Package、模型调用、Builder、工作台和身份/协作相关能力。

### 2.2 产品化缺口

- 尚未形成 Windows 安装包；
- 尚未自动创建桌面快捷方式和开始菜单入口；
- 尚未配置安装、卸载、升级和回滚流程；
- 尚未完成首次启动向导；
- 尚未完成安装包环境下的全功能真机验收；
- 当前 Runtime、UI 和未来同步/账号能力仍需进一步解耦。

## 3. 阶段一：Windows 桌面安装版

### 3.1 目标

生成普通用户可安装的 `Digital Me Setup.exe`，安装后可以通过桌面或开始菜单点击启动。

### 3.2 技术方案

采用 Electron + `electron-builder`（或等效 Electron 打包方案）生成 Windows NSIS 安装包。

交付内容：

- 安装包；
- 桌面快捷方式；
- 开始菜单入口；
- 卸载程序；
- 应用图标与版本信息；
- 安装后直接启动；
- 基础自动更新接口预留。

### 3.3 数据目录边界

应用程序与用户数据必须分离：

```text
应用程序：C:\Program Files\Digital Me\
用户配置：%APPDATA%\Digital Me\
主体资料：用户选择的 Digital Me 数据目录或默认用户目录
```

不得把可变 Package、原始素材、会话和密钥写入 `Program Files`。升级、卸载和重新安装不得默认删除用户主体资料。

### 3.4 首次启动向导

首次启动至少完成：

1. 创建或选择 Digital Me 数据目录；
2. 检查已有 Package 并提供导入入口；
3. 配置模型服务或选择后续配置；
4. 配置 API Key 并进行连接测试；
5. 说明数据保存位置和模型调用边界；
6. 进入默认工作台。

### 3.5 桌面版验收门槛

- 无 Node.js、npm、PowerShell 依赖；
- 新电脑可完成安装和首次启动；
- 无权限写入错误；
- 关闭并重新打开后数据仍在；
- 升级后 Package、配置和会话不丢失；
- 卸载时明确区分“卸载应用”和“删除个人数据”；
- 模型连接失败、路径错误和 Package 损坏均有可理解提示；
- Owner 完成真实安装、启动、配置、重启、升级和卸载验收。

## 4. 桌面版时间估算

### 4.1 建议估算

在现有 Electron 应用能够继续启动、当前功能范围不大幅扩张的前提下：

| 交付层级 | 预计开发时间 | 内容 |
|---|---:|---|
| 可安装内部测试版 | 3–5 个工作日 | 打包配置、安装包、快捷方式、基础启动验证 |
| 可交给早期用户试用版 | 7–12 个工作日 | 首次启动、数据目录、配置迁移、安装/卸载/升级测试 |
| 对外发布候选版 | 2–3 周 | 代码签名、发布流程、异常处理、干净机器与多轮回归 |

以上是工程时间，不包含等待 Owner 手工验收、证书申请、应用商店审核或外部账号配置的时间。

### 4.2 可能延长工期的因素

- 当前功能在几轮优化中继续调整主进程与 Package 结构；
- 安装包内资源路径与开发环境路径不一致；
- 原始素材和用户 Package 的迁移策略尚未固定；
- Electron 依赖升级或安全修复；
- Windows 代码签名证书申请；
- 需要同时支持 macOS 或 Linux；
- 需要加入自动更新、崩溃收集或多语言发布。

### 4.3 时间判断

因此，比较稳妥的对外判断是：

> **桌面安装启动的内部可用版本约 1 周，早期用户可试用版本约 2 周，较完整的发布候选版本约 2–3 周。**

## 5. 阶段二：Web 轻客户端

桌面安装版稳定后，先做 Web 轻客户端，而不是立即开发完整手机 App。

### 5.1 Web 端适合承载

- 轻量对话；
- 查看成果和成稿；
- 账号与设备管理；
- 加密同步入口；
- 授权确认；
- 任务状态与通知。

### 5.2 Web 端默认不承载

- 完整资料蒸馏流水线；
- 任意本机 MCP 进程托管；
- 重型文件处理；
- 完整 Package 编辑；
- 需要本地高权限的操作。

## 6. 阶段三：手机端

### 6.1 手机端定位

手机端是 Digital Me 的轻量控制面，不是桌面工作台的简单缩小版。

适合手机端的功能：

- 通知与任务完成提醒；
- 轻量对话；
- 随手输入文字、语音、照片和资料；
- 查看最近产物；
- 高风险行动确认或拒绝；
- 授权、停止和撤销；
- 出行场景下的快速交互。

默认不做：

- 完整 Package 编辑；
- 大规模资料蒸馏；
- 本地 MCP 进程安装；
- 重型研究、编程和文件工作台。

### 6.2 技术路线

手机端不能直接移植 Electron。应先形成共享的：

- Package 数据模型；
- Runtime 请求协议；
- 会话协议；
- 授权与审计协议；
- 加密同步协议；
- 模型网关接口。

之后采用手机薄客户端连接云边服务。若继续沿用 TypeScript / React 生态，可评估 React Native；也可在正式立项时比较 Flutter，最终以共享核心复用率和团队维护成本决定。

### 6.3 手机端前置基础设施

- 用户账号与设备信任；
- 端到端加密同步；
- Package 版本与冲突处理；
- 模型网关和配额；
- 推送通知；
- 授权确认接口；
- 协作和行动记录接口。

手机端正式开发前，必须明确：完整主体和原始素材默认不上传云端；云端主要承载账号、设备、加密数据、必要任务状态和用户明确授权的审计摘要。

## 7. 推荐实施顺序

```text
当前功能继续优化与验收
        ↓
Windows 桌面安装版
        ↓
Runtime / UI / 同步边界解耦
        ↓
Web 轻客户端
        ↓
手机端轻交互与高风险确认
        ↓
正式手机 App 与多端协作
```

## 8. 启动条件与暂缓事项

### 8.1 启动桌面产品化的条件

- 当前功能主路径完成至少数轮更新优化；
- Owner 完成现有 Electron 功能真机验收；
- Package 数据目录和迁移边界冻结；
- 现有应用的启动、退出、重启和配置保存路径稳定；
- 明确第一版只支持 Windows，避免同时扩大平台范围。

### 8.2 暂缓事项

在桌面安装版完成前，暂不启动：

- 完整手机 App；
- 完整云端人格库；
- 多端实时协作；
- 复杂自动更新平台；
- 以移动端为中心重写当前桌面产品。

## 9. 一句话决策

**先把 Digital Me 从“命令启动的开发工具”变成“安装后点击即可使用的桌面产品”，再以共享主体协议和加密同步为基础扩展 Web 与手机端。**

## 10. 修订记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-07-23 | v0.1 | 新建客户端产品化与多端演进计划；桌面安装版标记为后续 deferred 任务 |

