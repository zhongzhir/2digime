# Digital Me 唯一数字身份与对外协作：产品规划 v0.1（已废弃 superseded）

日期：2026-07-22  
状态：**superseded** — 已被 v0.2 取代

v0.2 地址：`DigitalMe_identity_collaboration_plan_v0.2_2026-07-22.md`

## 废弃原因

v0.1 基于技术研究（W3C DID/VC、AT Protocol、Solid）制定，但评审指出以下关键缺陷：

1. **身份与协作权重不足**：v0.1 把身份协作当作技术实现，评审要求提升为产品核心
2. **五种身份未区分**：v0.1 用一个"身份"概念覆盖所有场景，评审要求区分源身份/内在主体身份/角色身份/对外服务身份/任务会话身份
3. **协作定义不完整**：v0.1 聚焦凭据出示，评审要求完整的主体协作闭环（发现→任务→授权→执行→交付→反馈→关系）
4. **自主性边界不清**：v0.1 未定义自主性边界，评审要求区分认知自主/行动自主/身份自主
5. **"像我"定义过于抽象**：v0.1 未定义可验证的"像我"，评审要求场景化、证据化、授权化的身份一致性
6. **缺少主体协作闭环**：v0.1 无完整协作闭环，评审提供了具体的八步闭环
7. **缺少第一性对象**：v0.1 聚焦 DID/VC 实现，评审要求 Subject Identity、Role View、Intent、Authorization Grant、Interaction Contract、Evidence/Lineage 作为第一性对象
8. **验收指标不够全面**：v0.1 只有技术指标，评审要求身份清晰度/授权正确率/身份一致性/协作完成度/证据完整度/撤回有效性/演化可控性/普通人可用性

v0.2 已全面修订。  
上位依据：[`DigitalMe_product_baseline_reset_v0.2_2026-07-21.md`](DigitalMe_product_baseline_reset_v0.2_2026-07-21.md) §5.4  
研究依据：W3C DID/VC、AT Protocol、Solid、OpenAI Memory、Claude Personalization、MCP/A2A

---

## 一、为什么这是 Digital Me 的根本差异

用户如果只体验到"一个懂我的 AI 帮我写作和研究"（对话+做事），Digital Me 与 Claude、ChatGPT、Gemini 的差异几乎只在于个性化深度。真正的差异化来自：

1. 用户能看见一个**跨越工具和时间的我**，不是每次对话重新生成的临时上下文
2. 用户能**用一个凭证对外证明自己**（技能、身份、资质、偏好）而不依赖单一平台背书
3. 用户能**把"我"带到不同 AI 产品中使用**，而不是被锁在一个模型里
4. 用户能**撤销、冻结、迁移或删除**其数字之我，行为后果由本人承担

这四项必须在第一阶段就让普通用户明确感知到至少一项。

## 二、唯一数字身份的建立

### 2.1 身份的三层确认

| 层次 | 内容 | 确认方式 | 可信度 |
|---|---|---|---|
| **本人事实** | 经历、角色、成果、已核实声明 | 用户明确确认后标记为已核实 | 高——可被信任使用 |
| **系统推断** | 偏好、判断方式、表达风格 | 标注置信度，可被纠正 | 中——标注后可用 |
| **自我声明** | 想要成为什么样的人、发展方向 | 本人愿景，构成成长方向 | 声明——不验证，但可引用 |

### 2.2 身份标识

- **稳定标识**：每个用户有一个持久的 `did:dme:<hash>` 标识，不绑定单一平台或模型
- **本地生成**：标识在本地用密钥对生成，不依赖任何注册机构
- **可迁移**：标识和密钥对可以导出到任何兼容系统
- **可验证**：通过签名证明控制权，无需中心机构

### 2.3 身份数据结构

```
DigitalMeIdentity {
  id: "did:dme:a1b2c3...",
  publicKey: "...",
  createdAt: "...",
  updatedAt: "...",
  claims: [
    { type: "fact", text: "...", confirmedAt: "...", source: "..." },
    { type: "inference", text: "...", confidence: 0.85, lastUpdated: "..." },
    { type: "declaration", text: "...", declaredAt: "..." }
  ],
  credentials: [...],
  authorizations: [...],
  auditLog: [...]
}
```

## 三、对外凭据的出示

### 3.1 凭据类型

| 类型 | 用途 | 内容 | 隐私级别 |
|---|---|---|---|
| **完整凭据** | 本人查看/审计 | 全部 claims + 来源 + 历史 | 仅本人 |
| **精简凭据** | 对外协作 | 指定范围的 claims（如"AI 产品创始经验"） | 按目的最小化 |
| **零知识凭据** | 证明事实不泄露细节 | 证明"工作年限>5 年"不透露具体履历 | 最高隐私 |

### 3.2 凭据出示流程

```
用户选择出示范围 → 系统生成凭据包 → 用户确认 → 出示给对方
                                          ↓
                                    对方验证签名
                                          ↓
                                    用户可随时撤销
```

### 3.3 凭据的验证

- 对方收到凭据后，用公钥验证签名
- 验证通过则确认凭据来自该 DID 的持有者
- 凭据包含有效期，过期自动失效
- 用户可以随时撤销已出示的凭据

## 四、对外协作的分期

### 零期（即刻）：本人可见的连续自我呈现

- "我"的第一屏让用户感知到这是一个持续成长的自我
- 显示身份标识、已确认事实、近期学习
- 这是必须的——如果用户感觉不到"我"是持续存在的，后续协作无从谈起

### 一期：凭据出示与被调用

- 在受控场景中向外部服务出示精简凭据
- 授权外部 AI 服务调用 Digital Me 的特定能力
- 每次调用记录到审计日志
- 用户可以随时撤销授权

### 二期：被雇佣/接任务/匹配

- Digital Me 可以被外部 Agent 发现和调用
- 接任务、产出、交付、获得报酬
- 多 Digital Me 之间的协作网络

### 三期：多主体协作网络

- 多个 Digital Me 之间的自主交易与价值分配
- 跨组织的数字主体协作

## 五、技术路线

### 5.1 第一阶段（现在）：轻量实现

- **标识**：本地生成 Ed25519 密钥对，DID 用 `did:dme:<base58(publicKey)>` 格式
- **凭据**：JSON-LD 格式的 VC，用私钥签名
- **存储**：身份数据存在 Digital Me Package 中，与现有 Package 结构兼容
- **验证**：本地验证签名，不依赖外部 resolver
- **不做的**：不做 DID 钱包、不做区块链、不做 resolver 网络

### 5.2 第二阶段：互操作

- **AT Protocol 借鉴**：Merkle 树签名仓库用于 Package 版本化
- **VC 标准化**：凭据格式对齐 W3C VC 2.0，可被任何 VC 验证器验证
- **MCP 集成**：Digital Me 作为 MCP Server 暴露能力，外部 AI 可以通过 MCP 调用

### 5.3 第三阶段：网络效应

- **resolver 网络**：DID 解析到 DID Document
- **跨平台凭据**：凭据可以被其他 AI 产品验证
- **协作协议**：A2A 或类似协议用于 Agent 间协作

## 六、与现有代码的关系

### 6.1 复用现有资产

| 现有模块 | 复用方式 |
|---|---|
| `package-store/` | 身份数据存在 Package 中，复用版本化和迁移 |
| `subject-overview/` | "我"的概览展示身份和凭据 |
| `subject-overview/credentials.js` | 已实现的凭据生成/出示/撤销，升级为完整 VC |
| `policy-engine/` | 授权策略控制凭据出示范围 |
| `decision-audit/` | 凭据出示和撤销记录到审计日志 |
| `orchestration/` | 外部调用通过 L0 编排 |

### 6.2 新增模块

| 新模块 | 用途 |
|---|---|
| `identity/` | DID 生成、密钥管理、签名验证 |
| `identity/vc.js` | VC 签发、验证、组合 |
| `identity/resolver.js` | DID Document 解析（本地） |
| `collaboration/` | 外部协作接口、授权管理 |

## 七、最小可用形态（MVP）

第一阶段只做这些，让用户感知到差异：

1. **身份标识**："我"中显示 `did:dme:xxx`，可复制，说明这是你的唯一数字身份
2. **凭据生成**：基于已有资料生成精简凭据（已有 `credentials.js` 基础）
3. **凭据出示**：选择范围 → 生成 → 出示 → 记录（已有基础）
4. **凭据撤销**：随时撤销已出示的凭据（已有基础）
5. **审计日志**：所有凭据操作记录可查

不做：外部验证、跨平台互操作、Agent 市场、支付结算。

## 八、验收标准

1. [ ] 用户在"我"中看到自己的 `did:dme:xxx` 标识
2. [ ] 用户可以生成精简凭据（选择范围、有效期）
3. [ ] 凭据包含签名，可被本地验证
4. [ ] 用户可以撤销已出示的凭据
5. [ ] 所有凭据操作有审计记录
6. [ ] 身份数据随 Package 一起导出/迁移
7. [ ] 普通用户能理解"这是我的唯一数字身份"（不需要理解 DID 技术）

## 九、实现任务拆分

### Task ID-01：身份标识生成与展示

- 本地生成 Ed25519 密钥对
- DID 格式 `did:dme:<base58(publicKey)>`
- "我"中显示身份标识
- 标识随 Package 持久化

### Task ID-02：凭据升级为 VC 格式

- 凭据 JSON-LD 格式
- 私钥签名
- 本地验证
- 有效期和撤销

### Task ID-03：凭据出示流程

- 选择范围 → 生成 → 出示 → 记录
- 审计日志
- 撤销功能

### Task ID-04：身份与 Package 集成

- 身份数据存在 Package 中
- 随 Package 导出/迁移
- 版本化

---

## 十、不做什么

- 不做 DID 钱包
- 不做区块链
- 不做 resolver 网络
- 不做跨平台互操作（第二阶段）
- 不做 Agent 市场（第三期）
- 不做支付结算（第三期）
- 不做零知识证明（后期）
