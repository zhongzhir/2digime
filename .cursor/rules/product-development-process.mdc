---
description: 产品开发与用户面文案强制规范；数字主体第一纵向闭环阶段规则
alwaysApply: true
---

# 协作与文案强制规范

与规格 §2.12、`digitalme_context.md` §5.4、以及 `digitalme_subject_architecture_and_rd_principles_v0.1.md` 一致，会话中必须遵守。

## 一、用户面文案

界面、帮助、提示、空状态、进度、按钮与选项文案必须：

- **严谨、明白、中性**，面向最终用户与真实使用场景
- **禁止**把讨论腔、开发黑话、协议名、内部字段名、口语化口号写进默认产品面

自检：换成陌生普通用户，是否仍一眼明白？通不过则改写后再合入。

禁止示例：一等公民、grounded、DSML、tool_calls、claims、MCP/npx（默认路径）、养我/弄弄我。

## 二、Owner 意见 = 假设与线索

Owner / 用户提出的产品想法**可能不全、可错、可非最优**，不得直接当规格照搬。

### 强制顺序

1. **审视**：评估普遍性、完整性、风险；可反对并给更通用方案
2. **对标**：检索同类产品或行业最佳实践（主路径与明确不做）
3. **适配**：按 Digital Me 原则裁剪（主体层、能力跟随、少决策、本地优先）
4. **规格 / 决策**：写入产品规格与 context / log 后再排期
5. **实施**：按规格落地；冲突先改规格
6. **检测与审计**：验收用户路径；禁止协议泄漏、空壳按钮、无依据的「质检」宣称

### 禁止

- 把臆想流程或个人特例写成默认产品
- 无规格升版就堆功能
- 用文案按钮冒充能力却无可核对依据

## 三、数字主体第一纵向闭环阶段规则（2026-07-21 确立）

**权威入口**：

- 架构与研发原则：`digitalme_subject_architecture_and_rd_principles_v0.1.md`
- 当前执行计划：`digitalme_first_vertical_loop_sprint_plan_v0.1.md`（**`spec_frozen`**）
- 冻结规格：`digitalme_first_vertical_loop_spec_v0.1.md`
- 全局上下文文首：`digitalme_context.md`

**当前主线**：第一纵向闭环 — **理解我并产出**（研究与表达）。  
**下一项任务**：**第一闭环实现 · 第 2 块：真实 Skill 与只读外部调研调用**（须获 Owner 实现授权后才编码）。

1. **每次只推进一个已批准任务块**；按纵向闭环顺序，不得并列铺开 Skill / MCP / Agent / DID / 身份平台。
2. **非阻断问题进入 backlog**；仅四类问题可阻断：主流程不可用、数据损坏丢失、隐私超披露、未授权对外行动。
3. **用户面能力必须使用冻结状态标签**：可用 · 实验 · 本地模拟 · 预览 · 尚未开放。
4. **本地模拟不得写成真实外部协作**；失败与取消不得显示成功。
5. **工程完成 ≠ 用户面可用 ≠ accepted**；规格冻结 ≠ 已获实现授权。
6. **R0 / R1**：`accepted`，**retained as infrastructure**。  
7. **R2**：实现代码 **retained as infrastructure**；**停止**作为当前验收/边缘修复主线；不得因 R2 边缘问题阻塞第一闭环规划。  
8. **R3 renderer 迁移**：**`paused`**，**不是**下一步。  
9. **旧 DM-Core-01A「代表我完成任务」开发指令**：**`superseded`**（不得再按该指令扩展）。  
   提交 **`55ae01f`**：**`partially_reused_as_first_vertical_loop_scaffold`**（第 1 块已复用脚手架；固定截取与模型自述证据不作最终闭环）。
10. **PAN-02～PAN-06** 相对新主线 **`paused`**；不得按旧解锁条件自行启动。  
11. **P1-PANORAMA 执行索引**仅为历史/基础设施状态表，**不是**当前执行计划。  
12. Digital Org、公网协作、支付结算、DID/VC/链上存证不进入当前第一闭环实现。  
13. **未经 Owner 主路径验收不得 `accepted`**。  
14. **下一项**：**第一闭环实现 · 第 2 块：真实 Skill 与只读外部调研调用**（须获授权；不得在本规则中自行扩到第 2 块以外）。
