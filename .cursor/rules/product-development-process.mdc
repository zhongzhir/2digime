---
description: 产品开发与用户面文案强制规范；第二纵向闭环阶段规则
alwaysApply: true
---

# 协作与文案强制规范

与规格 §2.12、`digitalme_context.md` §5.4、`digitalme_subject_architecture_and_rd_principles_v0.1.md`，以及 `digitalme_owner_cto_ai_collaboration_principles_v0.1.md` 一致，会话中必须遵守。

## 一、用户面文案

界面、帮助、提示、空状态、进度、按钮与选项文案必须：

- **严谨、明白、中性**，面向最终用户与真实使用场景
- **禁止**把讨论腔、开发黑话、协议名、内部字段名、口语化口号写进默认产品面

自检：换成陌生普通用户，是否仍一眼明白？通不过则改写后再合入。

禁止示例：一等公民、grounded、DSML、tool_calls、claims、MCP/npx（默认路径）、养我/弄弄我。

## 一附、用户意图与确认负担（2026-07-26）

1. **内部领域对象不得自然转化为用户操作步骤**
2. **每增加一个确认、按钮或阶段，必须证明用户确实需要作出新的决策**
3. **没有新决策，就不得增加新确认**
4. **后台可保留复杂状态机，前台必须呈现简单的用户意图**
5. **默认主流程应尽可能一次操作完成**
6. **普通实现细节不得升级为 Owner 多轮确认**
7. **Owner 验收优先判断真实用户价值，而不是内部对象是否完整**

## 二、Owner 意见 = 假设与线索

Owner / 用户提出的产品想法**可能不全、可错、可非最优**，不得直接当规格照搬。

### 强制顺序

1. **审视**：评估普遍性、完整性、风险；可反对并给更通用方案
2. **对标**：检索同类产品或行业最佳实践（主路径与明确不做）
3. **适配**：按 Digital Me 原则裁剪（主体层、能力跟随、少决策、本地优先）
4. **规格 / 决策**：写入产品规格与 context / log 后再排期
5. **实施**：按规格落地；冲突先改规格
6. **检测与审计**：验收用户路径；禁止协议泄漏、空壳按钮、无依据的「质检」宣称

### 禁止

- 把臆想流程或个人特例写成默认产品
- 无规格升版就堆功能
- 用文案按钮冒充能力却无可核对依据

## 三、数字主体第二纵向闭环阶段规则（2026-07-26 更新；指针 2026-08-09）

**权威入口**：

- 架构与研发原则：`digitalme_subject_architecture_and_rd_principles_v0.1.md`
- 上位合同：`digitalme_phase1_task_DVL2-00_product_and_data_contracts_v0.1.md`（**v0.1.1** / `owner_accepted` / `frozen_for_implementation`）
- **当前实现分支**：`v2/foundation` · HEAD **`a851b0c`** · **未 push**
- 全局上下文文首：`digitalme_context.md`（决策 #110）
- 双机归档：`digitalme-v2/docs/audit/REMOTE-TWO-MACHINE-OWNER-VALIDATION-20260809.md`
- 通信路线图：`digitalme-v2/docs/design/SUBJECT-COMMUNICATION-REMOTE-ROADMAP.md`
- 双机清单：`digitalme-v2/docs/design/REMOTE-TWO-MACHINE-OWNER-CHECKLIST.md`
- 协作效率上位原则：`digitalme_owner_cto_ai_collaboration_principles_v0.1.md`

**当前主线**：第二纵向闭环 — **多模态成果包交付与渐进式主体构建**；V2 上三根主梁已有真实闭环；公网双真机协作**通路**已验证并归档；**协作功能扩展暂停**。

**当前指针（2026-08-09）**：今日文档归档收口。下一步决策门 = Owner/CTO「代表我—做事—协作」整系统复盘后再定优先级。**不得**自行启动 `REMOTE-COLLABORATION-DELIVERY-01` 或扩材料/Task/Artifact/Grant/多方/P2P。`a851b0c` 最小收口 **非** Owner accepted。**不得** push；**不得**宣称 `closed_alpha_ready` / `mvp_ready`。R3 **`paused`**；R2.5 **`deferred`**；PAN-02 **`blocked`**。P2C1 **`blocked`**（历史）。

1. **每次只推进一个已批准任务块**；不得偷跑或并列铺开。
2. **非阻断问题进入 backlog**；仅四类问题可阻断：主流程不可用、数据损坏丢失、隐私超披露、未授权对外行动。
3. **用户面能力必须使用冻结状态标签**：可用 · 实验 · 本地模拟 · 预览 · 尚未开放。
4. **本地模拟不得写成真实外部协作**；失败与取消不得显示成功；视频/音频本轮不得宣称已支持成品。
5. **工程完成 ≠ 用户面可用 ≠ accepted**；规格草案 ≠ 规格冻结 ≠ 已获实现授权；本机三进程 ≠ 真双机公网验收。
6. **R0 / R1**：`accepted`，**retained as infrastructure**。
7. **R2**：实现代码 **retained as infrastructure**；**停止**作为当前验收/边缘修复主线。
8. **R3 renderer 迁移**：**`paused`**，**不是**下一步。
9. **旧 DM-Core-01A「代表我完成任务」开发指令**：**`superseded`**。提交 **`55ae01f`**：**`partially_reused_as_first_vertical_loop_scaffold`**。
10. **第一纵向闭环**：`accepted` / `completed`；**不得**再写成下一待实施任务。
11. **PAN-02～PAN-06** 相对新主线 **`paused`/`blocked`**；不得按旧解锁条件自行启动。
12. **P1-PANORAMA 执行索引**仅为历史/基础设施状态表，**不是**当前执行计划。
13. Digital Org、支付结算、DID/VC/链上存证、广播市场、信誉、多方协作、P2P/NAT traversal **不进入**当前继续点。
14. **未经 Owner 主路径验收不得 `accepted`**；工程完成 ≠ Owner accepted（例：`a851b0c`）。
15. **下一项**：整系统阶段复盘决策门（不自行开协作扩展）。不得 push。R3 **`paused`**；R2.5 **`deferred`**；PAN-02 **`blocked`**；协作扩展 **`paused`**。
