---
description: 产品开发与用户面文案强制规范（Owner 线索须审视对标；文案严谨明白；Panorama 阶段规则）
alwaysApply: true
---

# 协作与文案强制规范

今日确认的两条原则，与规格 §2.12、`digitalme_context.md` §5.4 一致，会话中必须遵守。

## 一、用户面文案

界面、帮助、提示、空状态、进度、按钮与选项文案必须：

- **严谨、明白、中性**，面向最终用户与真实使用场景
- **禁止**把讨论腔、开发黑话、协议名、内部字段名、口语化口号写进默认产品面

自检：换成陌生普通用户，是否仍一眼明白？通不过则改写后再合入。

禁止示例：一等公民、grounded、DSML、tool_calls、claims、MCP/npx（默认路径）、养我/弄弄我。

## 二、Owner 意见 = 假设与线索

Owner / 用户提出的产品想法**可能不全、可错、可非最优**，不得直接当规格照搬。

### 强制顺序

1. **审视**：评估普遍性、完整性、风险；可反对并给更通用方案
2. **对标**：检索同类产品或行业最佳实践（主路径与明确不做）
3. **适配**：按 Digital Me 原则裁剪（主体层、能力跟随、少决策、本地优先）
4. **规格 / 决策**：写入产品规格与 context / log 后再排期
5. **实施**：按规格落地；冲突先改规格
6. **检测与审计**：验收用户路径；禁止协议泄漏、空壳按钮、无依据的「质检」宣称

### 禁止

- 把臆想流程或个人特例写成默认产品
- 无规格升版就堆功能
- 用文案按钮冒充能力却无可核对依据

## 三、P1-PANORAMA 阶段规则（2026-07-18 确立；2026-07-21 R2 第一轮有界修订后更新）

当前产品主线为 **P1-PANORAMA（三位一体 Alpha）**；执行索引见 `digitalme_panorama_execution_index_v0.1.md`（文内 **v0.2.19**）。PAN-00R 已 **accepted**。**PAN-01S / PAN-01S.1 / PAN-01S.2 已 `accepted`（baseline `cbde807`）。** Renderer Foundation R0 = **`accepted`**（v0.1.2）。Renderer Foundation R1 = **`accepted`**（任务包 **v0.1.3**；baseline **`8d7e9b3`**）。Renderer Foundation R2 = **`specified` / `codex_changes_requested` / `not_started`**（任务包 **v0.1.1-draft**；实现分支**不存在**）。

1. **每次只执行一个已批准的 PAN 主任务**；不得并行扩大实现范围。
2. **非阻断问题进入 backlog**，不因局部修饰延迟完整闭环。
3. **用户面能力必须使用冻结状态标签**：可用 · 实验 · 本地模拟 · 预览 · 尚未开放。
4. **本地模拟不得写成真实外部协作**；失败与取消不得显示成功。
5. **工程 `implemented` / `statically_verified` 不自动等于用户面「可用」**；自动测试通过不自动等于 `accepted`；**规格 `frozen_for_implementation` 不自动等于已获 Owner 实现授权或已开始编码**；**R1 `accepted` 不自动等于业务页面已迁移或整个 renderer 重构完成**；**R2 任务包起草/修订不自动等于已授权实现**。
6. **若需恢复暂停工作**（含 P1-07 缺口、原 P1-08、Policies 迁移等），先提交最小阻断说明，经复核后只做必要切片，**不可直接扩 scope**。
7. Digital Org、公网协作、支付结算不进入当前个人 Alpha 实现。
8. **当前唯一等待项：等待 Codex 再复核 Renderer Foundation R2 任务包。** 复核通过前不得创建 R2 实现分支或修改源码。R2.5 保持 `planned` / `deferred`。不得开始 R2 实现 / R2.5 / PAN-02。R0 / R1 已 accepted。PAN-02 保持 `planned` / `blocked`。
9. **未经 Owner 主路径验收不得 `accepted`**。PAN-02～PAN-06 未开始。不得自行修复已冻结的 P1-07。PAN-01 / PAN-01R 均已裁定不 accepted，不得回滚或删除其代码与证据。PAN-01R **无生产入口**（规格 v0.6.3）。
