# Digital Me 身份与主体协作开发计划 v0.1

日期：2026-07-22  
状态：`active` — 实施中  
上位依据：[`DigitalMe_identity_collaboration_plan_v0.2_2026-07-22.md`](DigitalMe_identity_collaboration_plan_v0.2_2026-07-22.md)

---

## 一、P0：战略定稿（文档交付）

### P0-1：一句话定义确认

**当前候选**：
> Digital Me 是源于本人、承载本人身份与意图、能够借助 AI 持续成长并在授权范围内进入数字世界协作的个人数字主体。

**必须同时包含**：身份/主体 + AI 增强 + 受控协作。

**交付物**：Owner 确认的一句话定义，写入 `digitalme_context.md`。

### P0-2：五种身份边界确认

| 身份类型 | 定义 | 产品表达 |
|---|---|---|
| 源身份 | 自然人及其对 Digital Me 的所有权、创建关系和最终控制权 | "我"中的身份标识、所有权证明 |
| 内在主体身份 | Digital Me 对"我是谁、重视什么、如何判断、不能做什么"的持续定义 | "我"中的自我呈现、价值观、边界 |
| 角色身份 | 创始人、投资人、写作者、家庭成员等场景化身份 | "我"中的角色选择、角色视图 |
| 对外服务身份 | Digital Me 向他人或 Agent 展示的能力、范围和联系入口 | 凭据、服务卡片 |
| 任务会话身份 | 某一次协作中的临时角色、授权范围、期限和可执行动作 | 任务授权、协作合同 |

**交付物**：五种身份的产品定义，写入本计划。

### P0-3：自主性三层边界确认

- **认知自主**：独立吸收信息、形成判断和提出新观点（无需确认）
- **行动自主**：在预先授权的范围内执行任务（需要授权）
- **身份自主**：不擅自改写源身份、价值边界、关键记忆、权利归属和长期目标（必须本人确认）

**交付物**：自主性边界的产品定义，写入本计划。

### P0-4：主体协作闭环产品级定义

八步协作过程：
1. 发现对方及其能力边界
2. 说明任务目标、背景、角色和预期结果
3. 明确可以使用哪些资料、记忆、能力和工具
4. 形成一次性或阶段性的授权
5. 双方执行、提问、协商和必要的人在场确认
6. 交付成果及来源、判断、修改和责任证据
7. 接受反馈、评价协作质量并决定是否形成长期关系
8. 结束、撤回、冻结或结算

**交付物**：主体协作闭环的产品定义，写入本计划。

---

## 二、P1：最小主体体验（实现任务）

### Task ID-01：身份标识生成与展示

**目标**：用户在"我"中看到自己的唯一数字身份标识。

**实现**：
- 本地生成 Ed25519 密钥对
- DID 格式 `did:dme:<base58(publicKey)>`
- "我"中显示身份标识，可复制
- 标识随 Package 持久化

**验收**：
- [ ] "我"中显示 `did:dme:xxx` 标识
- [ ] 标识可复制
- [ ] 重启后标识不变
- [ ] 普通用户能理解"这是我的唯一数字身份"

**测试**：`scripts/test-id-01-identity.cjs`

### Task ID-02：角色身份管理

**目标**：用户可以选择当前角色（创始人/投资人/写作者等），不同角色展示不同的身份视图。

**实现**：
- 角色选择界面（在"我"中）
- 角色视图配置（可见材料、表达方式、边界）
- 角色切换
- 角色随 Package 持久化

**验收**：
- [ ] "我"中可以选择角色
- [ ] 不同角色显示不同的身份视图
- [ ] 角色切换后，任务生成使用对应角色的上下文
- [ ] 重启后角色不变

**测试**：`scripts/test-id-02-role-view.cjs`

### Task ID-03：凭据升级为 VC 格式

**目标**：凭据从简单 JSON 升级为标准 VC 格式，包含签名和验证。

**实现**：
- 凭据 JSON-LD 格式
- 私钥签名（Ed25519）
- 本地验证
- 有效期和撤销

**验收**：
- [ ] 凭据包含 `@context`、`type`、`issuer`、`issuanceDate`、`credentialSubject`、`proof`
- [ ] 凭据包含 Ed25519 签名
- [ ] 本地可以验证签名
- [ ] 凭据包含有效期，过期自动失效
- [ ] 用户可以撤销凭据

**测试**：`scripts/test-id-03-vc-credential.cjs`

### Task ID-04：凭据出示流程

**目标**：用户可以选择凭据出示范围，生成凭据，出示给对方，并记录审计日志。

**实现**：
- 选择出示范围（完整/精简/零知识）
- 生成凭据
- 出示给对方（模拟）
- 审计日志记录
- 撤销功能

**验收**：
- [ ] 用户可以选择出示范围
- [ ] 凭据生成后显示出示确认
- [ ] 审计日志记录所有凭据操作
- [ ] 撤销后凭据失效

**测试**：`scripts/test-id-04-credential-flow.cjs`

### Task ID-05：主体协作闭环验证

**目标**：完成一次受控协作，验证身份、授权、协作、交付、反馈的完整闭环。

**实现**：
- 邀请另一个 Agent 或 Digital Me 进入任务（模拟）
- 问答、分工、交付、确认
- 查看来源、过程、修改和责任记录
- 反馈写回（人工确认）

**验收**：
- [ ] 用户可以邀请协作方
- [ ] 协作过程中可以查看对方能力边界
- [ ] 交付成果包含来源和责任记录
- [ ] 反馈写回前需要人工确认
- [ ] 反馈写回后可以撤回

**测试**：`scripts/test-id-05-collaboration.cjs`

---

## 三、P2：协作服务面扩展（后续）

- 受控对外展示
- 多次或阶段性授权
- 协作关系记忆
- 跨主体匹配
- 结算、出租和市场

---

## 四、实施顺序

1. **P0**（文档确认）→ 立即
2. **ID-01**（身份标识）→ P0 确认后
3. **ID-02**（角色身份）→ ID-01 完成后
4. **ID-03**（凭据 VC 升级）→ ID-02 完成后
5. **ID-04**（凭据出示流程）→ ID-03 完成后
6. **ID-05**（主体协作闭环）→ ID-04 完成后

每个任务完成后执行标准测试流程，通过后再开始下一个。

---

## 五、当前状态

- **P0**：待确认
- **ID-01**：待开始
- **ID-02**：待开始
- **ID-03**：待开始
- **ID-04**：待开始
- **ID-05**：待开始
