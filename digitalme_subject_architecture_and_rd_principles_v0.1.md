# Digital Me 数字主体架构与研发原则

版本：v0.1.1  
日期：2026-07-21  
状态：`active` / **当前最高架构与研发指导**  
权威关系：战略产品定义与研发治理以本文 + [`digitalme_context.md`](digitalme_context.md) 为准；**当前执行排期**以 [`digitalme_first_vertical_loop_sprint_plan_v0.1.md`](digitalme_first_vertical_loop_sprint_plan_v0.1.md) 为准。历史 P1-PANORAMA / Renderer Foundation 索引降为基础设施与历史排期记录。

---

## 0. 文档地位

1. 本文冻结 Owner 与 Codex 系统复盘后的**产品最高定义、系统框架、能力/身份原则、验收原则、纵向闭环与研发治理**。
2. 冲突时：
   - **产品是什么 / 长期架构循环** → 本文；
   - **近期唯一要证明什么 / 下一任务** → [`digitalme_first_vertical_loop_sprint_plan_v0.1.md`](digitalme_first_vertical_loop_sprint_plan_v0.1.md)；
   - **用户面文案与既有表面细则** → [`digitalme_product_spec_v0.2.md`](digitalme_product_spec_v0.2.md)（在与本文冲突处，以本文主线为准，规格正文标为待对齐）；
   - **部署与主权拓扑** → [`digitalme_architecture_edge_sovereign_v0.1.md`](digitalme_architecture_edge_sovereign_v0.1.md)。
3. 本文**不**等于已实现；预留接口与候选字段不得写成当前功能。

---

## 1. 最高产品定义

> **Digital Me 是由本人拥有和控制、以本人为源头持续形成、能够调用外部智能能力，并在明确授权下代表本人感知、判断、表达、行动和协作的个人数字主体系统。**

Digital Me **不是**以下任一单一产品的同义词：

- 更懂用户的聊天机器人；
- 安装很多工具的 Agent；
- 带 DID 的数字身份；
- 模仿用户文风的数字分身；
- 私人资料库；
- 通用 AI 工作台；
- 自动执行任务的个人助理。

### 1.1 必须同时实现的四项长期价值

| 价值 | 含义 |
|------|------|
| **连续的我** | 知道源自谁；跨时间一致、可追溯、可修正的主体模型 |
| **增强的我** | 可持续接入/替换模型、Skill、工具、MCP、外部 Agent，而不丢失「我」 |
| **受控的我** | 知道哪些信息可用、哪些能力可调、哪些行为必须经本人确认 |
| **发展的我** | 能从任务、结果、本人修正与外部反馈中成长；**未经确认的推测不得直接写成本人事实或观点** |

---

## 2. 数字主体循环（稳定系统框架）

后续架构与产品规划统一围绕五段循环：

```text
Subject Kernel
  → Intent & Authority
  → Capability Fabric
  → Action & Collaboration
  → Experience Loop
  → Subject Kernel
```

| 层 | 回答的问题 | 职责摘要 |
|----|------------|----------|
| **1. Subject Kernel** | 我是谁 | 本人事实、观点、价值、偏好、风格、能力、经验、角色、关系、目标、禁区、来源、置信度、冲突与版本 |
| **2. Intent & Authority** | 这次要什么、可代表到什么程度 | 任务目标、角色、允许使用的本人信息、允许调用的能力、对外披露范围、确认要求、失效条件 |
| **3. Capability Fabric** | 由谁、用什么、按什么方法完成 | 组织模型、Skill、MCP/Tool、外部 Agent；保留各自生命周期与信任模型 |
| **4. Action & Collaboration** | 如何产出与行动 | 成果生成、工具操作、任务委派、未来对外代表行为 |
| **5. Experience Loop** | 如何回流而不污染主体 | 区分任务事实、产物、本人修正、外部反馈、系统推测、可复用方法；仅规则允许或本人确认的变化可进入主体模型 |

---

## 3. 能力接入原则

不得再把 Skill、MCP、Agent 当成三个完全相同的插件类型：

| 类型 | 回答 | 边界 |
|------|------|------|
| **Skill** | 怎样做 | 方法、知识、工作流程 |
| **MCP / Tool** | 能读取/操作什么 | 数据与工具连接 |
| **Agent** | 可以交给谁做 | 可被委派的外部执行主体 |

可共享：发现、登记、授权、运行记录、结果引用等上层结构。  
**必须保留**：不同的数据合同、信任边界与运行方式。

目标不是「能安装、能调用」，而是：

> 在不丢失主体连续性、不越过本人授权、不无差别披露个人信息的前提下，低摩擦地使用不断变化的最好外部能力。

---

## 4. 身份原则

不得将「唯一身份」简化为 UUID、DID 或密钥。身份至少包含：

1. **主体连续性**：是否为同一个 Digital Me；
2. **本人控制关系**：属于谁、由谁控制、谁能冻结和修改；
3. **对外角色**：本次以什么角色、能力与范围出现；
4. **行动授权**：这一次具体获准代表本人做什么。

近期须预留：稳定 `subjectId`、本人控制关系、任务授权、行为证据。  
**DID / VC / 链上存证 / 结算** 仅可作为以后可插拔的外部证明与交易层，**不得阻塞**当前产品闭环。

---

## 5. 「理解我、代表我」验收原则

不是静态页面，也不是把 persona / memory / framework / style 全部注入 prompt。

用户感知须由**可验证行为**建立：

- 能选择当前任务真正相关的本人信息；
- 能展示使用了哪些本人事实、观点、偏好与边界；
- 不把系统推测伪装成本人立场；
- 能说明结果为什么符合本人；
- 依据不足时明确承认不确定；
- 用户可以纠正；
- 纠正后，下一次相似任务确实发生改变；
- 对外表达时区分本人既有观点、外部事实与 Digital Me 新推断；
- 不因模型或外部能力变化而丢失主体连续性。

**核心对照测试**：同一任务分别交给通用 AI 与 Digital Me，Owner 能否稳定识别 Digital Me 更符合本人的事实、判断框架、表达方式与行为边界。

---

## 6. 纵向闭环（禁止横向模块堆积）

| 闭环 | 证明什么 | 当前状态 |
|------|----------|----------|
| **第一闭环：理解我并产出** | 用少量可核对本人信息 + 真实外部能力完成真实成果，解释为何符合本人，并接收纠正 | **当前唯一规划与实施范围** |
| **第二闭环：受控委派** | 边界明确的子任务交给外部 Agent，必要披露，收回结果，本人决定采用/拒绝 | 仅保留接口与方向 |
| **第三闭环：代表本人对外协作** | 稳定身份、明确角色、单次授权；低风险可撤回；留行为证据 | 仅保留接口与方向 |
| **第四闭环：从反馈中成长** | 确认后的纠正影响下次相似任务；保留旧版本、依据、适用范围与冲突 | 仅保留接口与方向 |

后续闭环**不得提前铺开建设**。

---

## 7. 研发治理

### 7.1 每个任务块必须写明

- 服务哪个纵向闭环；
- 完成后 Owner 能看到和操作什么；
- 复用了哪些现有结构；
- 新增了什么最小合同；
- 什么问题允许阻断；
- 什么问题只记录、不阻断。

### 7.2 主循环

1. 冻结产品结果、主流程与核心合同；  
2. 集中实现；  
3. 集中代码复核；  
4. Owner 主流程验收；  
5. 非阻断问题统一进 backlog；  
6. 进入下一任务块。

### 7.3 仅下列问题允许阻断

1. 主流程无法完成；  
2. 个人数据损坏或丢失；  
3. 隐私正文泄露或超出任务需要披露；  
4. 未经授权代表本人对外行动。

### 7.4 默认不得阻断近期闭环

低频竞态、非关键路径极端异常、页面不够精致、动画/菜单/提示文案、用户感知不到的架构优化、为未来预先建设的完整平台能力。

### 7.5 Owner 意见仍是线索

继续遵守：审视 → 对标 → 适配 → 写入规格/决策 → 实施 → 检测。不得把讨论中的具体功能点未经研究直接写成系统架构与开发优先级。

---

## 8. 与既有计划的关系（摘要）

| 项 | 状态 |
|----|------|
| R0 / R1 | `accepted`；**retained as infrastructure** |
| R2 | 实现已完成但**不再追加边缘验收为主线**；代码 **retained as infrastructure**；Owner 是否补标 `accepted` 不阻塞新主线 |
| R3 renderer 迁移 | **`paused`**；**不是**下一项任务 |
| R2.5 / SQLite | `planned` / `deferred`；非当前主线 |
| PAN-02～PAN-06 | **`paused` / `blocked` 相对新主线**；历史索引保留 |
| 旧「Skill/MCP/Agent/身份并列 7 任务块」 | **`superseded`** |
| **旧 DM-Core-01A 开发指令** | **`superseded`**（不得再按该指令扩展；见 `digitalme_dm_core_01a_superseded_notice_v0.1.md`） |
| **提交 `55ae01f`（act-behalf）** | **`retained_for_mapping_review`** / `experimental_infrastructure`（已存在实现；**不是**第一闭环完成态；本次不得改代码） |
| P1-PANORAMA 执行索引 | **历史执行记录 + 基础设施状态表**；**不再声称「当前产品主线」** |

细则与合同映射见 [`digitalme_first_vertical_loop_sprint_plan_v0.1.md`](digitalme_first_vertical_loop_sprint_plan_v0.1.md)。

---

## 9. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1.0 | 2026-07-21 | 规划基线重建：写入最高定义、五段循环、能力/身份/验收/治理；废止旧并列任务与 DM-Core-01A 执行指令 |
| v0.1.1 | 2026-07-21 | 澄清：旧指令 `superseded` ≠ 否认提交 `55ae01f`；实现为 `retained_for_mapping_review` |
