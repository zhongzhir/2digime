# Digital Me：人的定义与数据采集口径 v0.1

版本：v0.2.8  
日期：2026-07-12  
状态：产品设计基线（指导「我」栏目与 Package 收集口径）  
关联：`digitalme_context.md` §2.4–2.5、`digitalme_life_graph_v0.1.md`、`digitalme_material_guidance_v0.1.md`、`digitalme_product_spec_v0.2.md` §3 / §6、`digitalme_data_sovereignty_principle_20260711.md`

---

## 0. 一句话

**Digital Me 收集并分析利用的，是「人」可被数字化的观念、事实与行为中：可记录、可授权、可纠错的子集。**  
目标不是抄履历，而是形成**可代理的自我模型**，且始终遵守：**有 ≠ 会说**。

大方向（蒸馏 + 社会事实 + 仅保管 + 本地主权）不变；本文补齐完整性、精确度、友好度与操作性。

---

## 1. 理论抽象：七层可数字化定义

综合符号互动论（米德主我/客我）、角色与拟剧论（戈夫曼前台/后台）、惯习与资本（布迪厄）、叙事认同（McAdams / Ricoeur）、关系性人格（人类学 personhood），以及本项目已有管道（`digitalme_context.md` §2.5），压成七层：

| 层 | 名称 | 理论来源 | 记什么 | Package / 产品落点 | 分期 |
|----|------|----------|--------|-------------------|------|
| L1 | 观念与判断 | 客我内化、价值观、决策框架 | 立场、取舍理由、原则 | `persona`、`decision-frameworks`、memory | ✅ 蒸馏 |
| L2 | 表达与风格 | 戈夫曼前台、惯习外显 | 怎么说、语气、结构 | `style-guide` + RAG 示例 | ✅ 蒸馏 |
| L3 | 社会角色与地位 | 角色理论、地位—角色集 | 何时、何职、何机构 | `life/events` + `life/roles` | ✅ 需可编辑 |
| L4 | 关系自我 | 关系性人格；自我在互动中生成 | **相对某人**：亲疏、义务、沟通模式 | 新 ego–alter 模型（**非**旧 relations） | ⏳ 口径定、UI 后置 |
| L5 | 人生叙事时间 | 叙事认同；时间中的连续自我 | 生命事件主线 | **真时间轴** + `events.jsonl` | ✅ 本轮主战场 |
| L6 | 行为与实践 | 实践论；行为 vs 自我报告 | 日程、选择、节律 | 行为管道（架构预留） | ⏳ 后置 |
| L7 | 边界与后台 | 前台/后台；数据主权 | 永不代理、仅保管、高后果确认 | `policies/boundaries` + 保管库 | ✅ 系统默认 |

```text
时间轴 (L5) ──派生──► 角色 (L3)
              └──未来─► 关系 (L4)
观念材料 ──► L1 + L2（观念与表达）
闸门 L7 ──约束──► L1 / L3 / L4 的注入与对外代理
```

---

## 2. 数据采集口径（用户动作 → 系统动作）

| 口径（用户面） | 对应层 | 典型材料 | 系统动作 |
|----------------|--------|----------|----------|
| **丰富表达与判断** | L1–L2 | 文章、方案、带理由的笔记、问卷、修正 | 蒸馏 → 审阅勾选 → 写入；宜可编辑 |
| **登记人生事实** | L3、L5（及未来 L4） | 简历、任职证明、项目参与证明 | 提取事件 → **落到时间轴**；轴上可改 |
| **仅保管** | L7 | 证件、密码、未脱敏他人信息、声明禁入项 | 本地保管；默认不注入对话 |

文案约束：默认界面用「丰富表达与判断 / 登记人生事实 / 仅保管」，避免把研讨个案写进示例（见产品规格 §2.10）。

---

## 3. 关系（L4）口径草案（本轮不做 UI）

一条关系必须满足：

```text
me + alter（自然人，非机构）
+ relationType（亲属 / 同事 / 师徒 / 合作 / 好友 …）
+ asymmetricRoles（我对 TA / TA 对我）
+ context + when
```

| 规则 | 说明 |
|------|------|
| 机构归属 | 归 **L3 角色**，不进关系表 |
| 无明确 alter | **禁止**自动「生成关系」 |
| 旧 `life/relations.json` | 由机构 actors 派生，**不符合**本口径；产品面下线；文件可保留不展示 |

---

## 4. 表达禁区（L7）系统默认清单

原则：**出厂即有默认规定**；用户修改/关闭默认项须**显式确认**；可「恢复系统默认」。

| id（逻辑键） | 默认文案（通用） | 建议 scope |
|--------------|------------------|------------|
| `sys_credentials` | 证件号、账号密码、支付与银行卡等凭证细节 | never_speak_for_me + never_export |
| `sys_others_privacy` | 未经同意的他人隐私与可识别信息 | never_speak_for_me |
| `sys_medical` | 详细医疗病历与未授权健康细节 | never_inject + never_speak_for_me |
| `sys_finance_raw` | 未授权的具体财务数字与持仓明细 | never_speak_for_me + never_export |
| `sys_legal_commit` | 未经本人确认的对外法律意见、签约与正式承诺 | never_speak_for_me |
| `sys_intimate` | 亲密关系与家庭内部敏感细节（除非本人当次明确要求） | never_speak_for_me |
| `sys_location_precise` | 精确住址与实时行踪（除非本人当次明确要求） | never_inject + never_export |

条目字段建议：`id, text, scope, source: system|user, enabled: true|false, createdAt, updatedAt`。  
`source=system` 且被用户关闭/改写时，须确认；注入对话时只使用 `enabled !== false` 的项。

---

## 5. 对标摘要（借鉴点）

| 对象 | 可借鉴 | Digital Me 须守住的差异 |
|------|--------|-------------------------|
| Second Me | 本地身份记忆、主权叙事 | DM 另有社会事实、禁区、产物工作台 |
| Memoral / MemPoint / Memex | 自然语言进 → 结构出；时间卡片 | DM 要「像我做事」+ 授权边界 |
| LifeScribe / LifeChapter | **可视化人生时间轴**、里程碑增删 | 轴上节点进 Package 并驱动对话 |
| Personal Digital Twin 文献 | 精神/身体/社会/生物多域 | 近期仍以认知 + 社会事实为主 |
| AI&Society 批评 | 「数字孪生」易过度承诺 | 话术用「数字之我 / 可授权自我模型」 |
| Solid / digi.me | 个人数据仓与授权 | 基础设施；DM 是主体层应用 |

工程优先启发：**时间轴交互** + **结构抽取** + **本地主权**；关系等模型成熟再开。

---

## 6. 与「我」栏目的映射

| 「我」子页 | 服务层 | 用户能做什么 |
|------------|--------|--------------|
| 概览 | 全层覆盖度 | 一眼看到观念/事实/边界是否就绪 |
| **认知** | L1–L5 + 推断消费面 | 观念 / 成就 / 社会关系（关系人 vs 机构触点）/ 能力边界可视化；生成自我认知简报 |
| 时间线 | L5（派生 L3） | 真轴上添加/编辑/删除；**从材料登记人生事实** |
| 角色 | L3 | 浏览任职切片，点回事件 |
| 观念与表达 | L1–L2 | 仅丰富表达与判断 + 问卷；仅保管 |
| 边界 | L7 | 系统默认；修改须确认 |
| 材料 | 映射入口 | 投递箱 + 可读范围；富化后写入可调用画像 |
| 关系 | L4 | 认知页提供关系人候选与手动添加；完整关系 UI 可加深 |

侧栏名：**「我」**。取消并列的「人生轨迹」「蒸馏」导航。

---

## 7. 推进计划：从「用户先分类」到「一扔就建」

### 7.1 判断

要求用户先想清楚「这是观念还是事实」再导入，**摩擦过高**，也不符合「人人可用 · 少决策」。  
理想路径（你描述的）正确：

> 用户丢一堆材料（文章、工作文档、邮件、聊天记录）或授权访问本机/手机 → 系统建框架、归类、抽取 → 审阅确认 → 数字之我成型。

这与既定三循环（映射 / 应用 / 反馈）一致：**映射循环的产品化入口**应是「材料投递箱」，而不是三个并列单选。

### 7.2 分期（建议顺序）

| 阶段 | 用户感知 | 工程要点 | 优先级 |
|------|----------|----------|--------|
| **A. 入口对齐（已做）** | 事实进时间线；观念进「观念与表达」 | 降低逻辑错位 | ✅ |
| **B. 材料投递箱** | 「我」内统一投递：拖文件/多选 → **系统建议**归入观念 / 事实 / 保管 → 用户一键确认或微调 | 分类器（规则+模型）；审阅仍人工确认；禁止静默写入高敏 | ✅ B1 已落地 |
| **C. 文件夹映射** | 指定本机文件夹（如「文档/工作」），定期增量扫描 | 本地授权范围；去重；入投递箱待确认 | ✅ C1 手动扫描已落地 |
| **F. 人模型富化 + 可调用** | 材料确认后更新事件/推断/成就/触点/能力线索；对话与认知面板真正调用 | PersonEnrichment；写入即注入/检索 | ✅ 首期薄层 |
| **G. 自我认知面板** | 「我 · 认知」：观念/成就/社会关系/能力边界 + 简报 | 覆盖度条、触点图、产物库报告 | ✅ 首期薄层 |
| **D. 渠道接入** | 邮件/微信等经「能力」授权接入，仍落投递箱 | MCP/OAuth；默认不读全盘 | 体验优先但须授权清晰 |
| **E. 手机/跨端** | 薄客户端投递 → 端主权 Runtime | 架构见 edge-sovereign；UI/Runtime 解耦后 | 后置 |

### 7.3 投递箱算法原则（B/F 阶段设计约束）

1. **默认建议、人工确认**：系统给出分流建议与置信度；写入仍勾选确认（与现审阅一致）。  
2. **可拆分**：同一文件可「事实段 → 时间轴 + 议论段 → 观念」（拆管道，不整文硬套一种）。  
3. **高敏保守**：证件/财务/他人隐私倾向「仅保管」或禁区，宁可少说。  
4. **增量可重建**：原始材料进素材库；Package 为蒸馏/抽取结果；可重跑。  
5. **不假装读完整个手机**：全盘扫描不做默认；授权范围要说人话。  
6. **用起来才算收录**：写入 Package 的切片须至少进入系统提示摘要、工作台检索、认知面板或可生成报告之一。  
7. **通用信号词典**：活动/履历/成果/论述/凭证等类别举一反三，禁止只为某一文件名写死业务逻辑。

### 7.4 与竞品差异（守住）

Mem* / LifeScribe 偏日记与纪念；Second Me 偏记忆代理。Digital Me 投递箱的终点是**可授权自我模型**（能做事、有边界），不是个人 wiki。

---

## 8. 决策编号

见 `digitalme_context.md` 决策 **#30**（人的定义七层与「我」栏目）、**#31**（投递箱为主构建路径）、**#33**（人模型富化可调用 + 认知面板）。
