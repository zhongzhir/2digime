# Digital Me "做事" 产品逻辑 v0.1（已废弃 superseded）

日期：2026-07-22  
状态：**superseded** — 已被 v0.2 取代

v0.2 地址：`DigitalMe_doing_product_logic_v0.2_2026-07-22.md`

## 废弃原因

v0.1 提出"不做任务类型选择器"的方案过于绝对。Owner 指出：每个做事的能力要么独立达成 95 分位，要么通过与其它工具的协作达成同样目标。编程等能力不能独立达成 95 分位，必须通过协作路径。v0.2 按此重新定位。

---

## 一、核心原则

**"做事"是统一任务执行环境，不是任务类型堆叠区。**

用户只需要理解：描述目标 → 得到初稿 → 修改 → 输出。不需要理解"这是写作场景还是研究场景"。

基线 v0.2 §5.2 已明确：写作、研究等只是快捷模板，不是不同的复杂产品世界。同一任务中的研究、写作、编程、质检和导出由后台组合，用户看到的是一个连续工作对象。

## 二、用户视角的"做事"流程

### 2.1 统一入口

```
用户描述目标 → 系统自动理解任务类型 → 直接生成初稿 → 用户审阅修改 → 输出成果
```

**不需要模板选择。** 系统根据用户描述自动判断任务类型（写作/研究/编程/其他），路由到合适的后台能力组合。

模板只是快捷入口（可选），不是必经步骤。用户也可以直接描述目标，系统自动判断。

### 2.2 任务界面布局

```
┌─────────────────────────────────────┐
│  任务标题（可选）                      │
│  任务目标（必填）                      │
│  [直接生成初稿]                       │
│                                     │
│  高级：查看/调整本次使用的本人信息 ▸     │  ← 默认折叠
│                                     │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │      初稿内容显示区            │   │  ← 成果是第一视觉焦点
│  │      （可编辑）               │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
│  [保存修改] [采用] [否定] [查看依据]   │
│                                     │
│  依据面板（点击"查看依据"后展开）      │  ← 按需可见
│  - 本次如何考虑了我                   │
│  - 外部事实及来源                     │
│  - Digital Me 的新推断               │
└─────────────────────────────────────┘
```

### 2.3 任务类型与后台能力

| 任务类型 | 用户看到 | 后台自动组合 |
|---|---|---|
| 写作 | 初稿文本 | 上下文选择 + 写作模型 + 质检 |
| 研究 | 答案+来源 | 上下文选择 + 外部检索 + 分析模型 + 来源整理 |
| 编程 | 代码初稿 | 上下文选择 + 代码模型 + 语法检查 |
| 文件处理 | 处理结果 | 上下文选择 + 文件解析 + 处理模型 |
| 自定义 | 通用初稿 | 上下文选择 + 通用模型 |

**关键**：用户不需要选择任务类型。系统根据目标描述自动判断。模板只是帮助用户快速开始的快捷方式。

## 三、历史任务

### 3.1 任务列表

- 显示所有正在进行的任务和最近完成的任务
- 按更新时间排序，最近在前
- 每个任务显示：标题、状态、更新时间、目标摘要
- 点击进入任务详情页，可以继续修改、重新生成、输出

### 3.2 任务详情页

进入历史任务后：
- 恢复任务标题、目标、角色、期望成果
- 显示已有的初稿（如有）
- 可以继续编辑初稿
- 可以重新生成（修改目标后）
- 可以保存修改、采用、否定
- 可以查看依据、导出成果

## 四、左侧导航

```
对话    ← 快速问答、轻任务
做事    ← 统一任务执行环境（原"工作台"改名）
我      ← 数字自我、身份、凭据
能力    ← 能力管理（二层）
```

"做事"替代"工作台"，因为：
- "工作台"暗示多个独立工具/场景
- "做事"强调统一任务执行，符合基线"查看和继续正在做的事"

## 五、不做什么

- 不做任务类型选择器（系统自动判断）
- 不做独立的写作/研究/编程场景（统一入口）
- 不在默认路径上显示后台机制（上下文候选、调研过程、证据列）
- 不要求用户理解任务分类

## 六、实现改动

### 6.1 导航改名

- 左侧导航"工作台" → "做事"

### 6.2 移除模板选择器

- 删除"新建任务"弹出的模板选择
- 改为直接打开空白任务表单

### 6.3 统一任务界面

- 所有任务使用同一个 act-behalf 界面
- 根据任务类型自动调整后台能力组合（不需要用户选择）
- 初稿显示区根据任务类型适配（文本/代码/答案）

### 6.4 历史任务

- 任务列表显示所有任务
- 点击进入任务详情页，恢复完整状态
- 可以继续修改、重新生成、输出

### 6.5 智能路由（后台）

- `task-intent.js` 增加任务类型自动判断
- 根据目标描述关键词路由到不同后台能力
- 写作 → 写作模型
- 研究 → 检索 + 分析模型
- 编程 → 代码模型
- 其他 → 通用模型

## 七、验收标准

1. [ ] 左侧导航显示"做事"而非"工作台"
2. [ ] 进入"做事"看到任务列表，点击任务进入详情页
3. [ ] 新建任务不需要选择类型，直接描述目标
4. [ ] 点击"直接生成初稿"得到结果，不需要选择任务类型
5. [ ] 初稿是第一视觉焦点，依据面板按需展开
6. [ ] 历史任务可以恢复完整状态，继续修改和输出
