# Digital Me 身份与主体协作产品规划 v0.2

日期：2026-07-22  
状态：`draft` — Owner 审核后进入实现  
上位依据：[`DigitalMe_product_baseline_reset_v0.2_2026-07-21.md`](DigitalMe_product_baseline_reset_v0.2_2026-07-21.md) §5.4  
评审依据：[`digitalme_重要事项与建议_身份协作评审.md`](digitalme_重要事项与建议_身份协作评审.md)  
研究依据：W3C DID/VC、AT Protocol、Solid、OpenAI Memory、Claude Personalization、MCP/A2A

---

## 一、为什么身份与协作是 Digital Me 的产品核心

Digital Me 的战略方向已经形成了区别于普通 AI 助手、数字分身、知识库和 Agent 平台的独特命题：

> Digital Me 不是替人做事的外部超能助手，而是源于本人、承载本人身份与意图、能够借助 AI 持续成长并在授权范围内进入数字世界协作的"个人数字主体"。

当前产品主线容易被理解为：导入资料 → 生成画像 → 调用 AI → 产出文件。这个闭环可以证明"蒸馏"有效，但还不能充分证明 Digital Me 的独特性，因为个人知识库、记忆型助手和 Agent 工作台也可以完成相似流程。

Digital Me 应当从产品定义上多出一个关键闭环：

> **认识我 → 确认我当前以什么身份做什么 → 代表我与其他主体协作 → 将过程、结果和反馈回流到我。**

因此，Builder、Runtime、Package 不是三个平行模块，而应共同服务于一个主体闭环：

| 层次 | 关键问题 | Digital Me 的产品表达 |
|---|---|---|
| 来源层 | 我是谁、从哪里来 | 自然人、来源材料、本人确认 |
| 主体层 | 我如何记忆、判断、表达和成长 | 身份连续性、意图、价值、边界、记忆、反馈 |
| 行动层 | 我现在能做什么 | 能力、工具、任务、产物和证据 |
| 协作层 | 我如何与谁合作 | 角色、关系、授权、任务合同、交付、撤回 |
| 网络层 | 我如何被发现、调用和获得价值 | 公开身份、能力面、信誉、结算和市场 |

近期产品不必实现完整网络层，但必须让用户看见前四层之间的连接。否则"数字主体"会被用户感知为"资料加工后的 AI 助手"。

## 二、五种身份的边界

现有材料中"身份""人格""数字之我""对外 Agent 身份"有时互相替代。实现时必须明确区分：

1. **源身份**：自然人及其对 Digital Me 的所有权、创建关系和最终控制权；
2. **内在主体身份**：Digital Me 对"我是谁、重视什么、如何判断、不能做什么"的持续定义；
3. **角色身份**：创始人、投资人、写作者、家庭成员等场景化身份；
4. **对外服务身份**：Digital Me 向他人或 Agent 展示的能力、范围和联系入口；
5. **任务会话身份**：某一次协作中的临时角色、授权范围、期限和可执行动作。

这五种身份应当有继承关系，但不能混为一个不可分割的"人格包"。同一个人可以有多个角色和多个对外服务面；不同协作对象看到的也应是不同的、经过授权的身份视图。DID、Agent Card 或其他协议最多解决寻址、声明和验证问题，不能替代 Digital Me 对"我是谁"的内部建模。

### 2.1 身份的三层确认

| 层次 | 内容 | 确认方式 | 可信度 |
|---|---|---|---|
| **本人事实** | 经历、角色、成果、已核实声明 | 用户明确确认后标记为已核实 | 高——可被信任使用 |
| **系统推断** | 偏好、判断方式、表达风格 | 标注置信度，可被纠正 | 中——标注后可用 |
| **自我声明** | 想要成为什么样的人、发展方向 | 本人愿景，构成成长方向 | 声明——不验证，但可引用 |

### 2.2 身份标识

- **稳定标识**：每个用户有一个持久的 `did:dme:<hash>` 标识，不绑定单一平台或模型
- **本地生成**：标识在本地用密钥对生成，不依赖任何注册机构
- **可迁移**：标识和密钥对可以导出到任何兼容系统
- **可验证**：通过签名证明控制权，无需中心机构

### 2.3 身份数据结构

```
DigitalMeIdentity {
  id: "did:dme:a1b2c3...",
  publicKey: "...",
  createdAt: "...",
  updatedAt: "...",
  // 源身份
  sourceIdentity: { ownerId, createdBy, ownershipProof },
  // 内在主体身份
  subjectIdentity: {
    claims: [
      { type: "fact", text: "...", confirmedAt: "...", source: "..." },
      { type: "inference", text: "...", confidence: 0.85, lastUpdated: "..." },
      { type: "declaration", text: "...", declaredAt: "..." }
    ],
    values: [...],
    boundaries: [...]
  },
  // 角色身份
  roleViews: [
    { role: "founder", scope: "...", visibleClaims: [...], expressionStyle: "..." },
    { role: "investor", scope: "...", visibleClaims: [...], expressionStyle: "..." }
  ],
  // 对外服务身份
  serviceProfiles: [
    { audience: "...", capabilities: [...], contactEndpoint: "..." }
  ],
  credentials: [...],
  authorizations: [...],
  auditLog: [...]
}
```

## 三、自主性的三层边界

《数字之我》系统构想中关于"数字之我走上自己的独立道路""成为另一个我"的判断，是项目最具创新性的思想来源之一，应保留，不宜被安全术语稀释。但必须明确一条内在边界：

> Digital Me 可以独立学习、推理、提出方案、生成新经验和在授权范围内行动；但不能未经本人确认，擅自改写源身份、价值边界、关键记忆、权利归属和长期目标。

建议把"自主性"拆为三层：

- **认知自主**：独立吸收信息、形成判断和提出新观点；
- **行动自主**：在预先授权的范围内执行任务；
- **身份自主**：不应默认拥有修改本人根本身份和权利边界的权力，必须由本人定义或确认。

这样既能保留"从人出发但不止于人的数字实体"这一战略想象，也能形成可实现、可验收的产品边界。

## 四、"像我"的场景化定义

"与真人高度相似""本我接纳数字之我即为真我"适合作为远景宣言，不宜直接作为近期产品承诺。近期应验证的不是抽象的"像不像"，而是：

- 在同一角色和任务范围内，是否持续遵循本人已确认的价值、偏好和表达方式；
- 是否能区分事实、本人观点、系统推断和外部新信息；
- 面对不确定问题时，是否使用本人的判断框架而不是假装知道；
- 是否能说明某项判断和输出来自哪些材料、反馈或新经验；
- 是否在跨会话、跨模型、跨工具后仍保持身份连续性；
- 是否在授权撤回后停止继续使用相应范围的资料和能力。

建议产品将"像我"定义为**受场景、角色、证据和授权约束的身份一致性**，而不是泛化的仿真人格评分。

## 五、主体协作闭环

协作不是一个 Agent 调用另一个 Agent 的接口，而是一段有关系、有意图、有边界、有结果责任的过程：

1. 发现对方及其能力边界；
2. 说明任务目标、背景、角色和预期结果；
3. 明确可以使用哪些资料、记忆、能力和工具；
4. 形成一次性或阶段性的授权；
5. 双方执行、提问、协商和必要的人在场确认；
6. 交付成果及来源、判断、修改和责任证据；
7. 接受反馈、评价协作质量并决定是否形成长期关系；
8. 结束、撤回、冻结或结算。

建议把这一过程称为 **主体协作闭环**。它比"协作网关"更接近产品本质，也能成为未来 A2A、MCP、AP2 等协议的上层产品逻辑。协议是连接方式，主体协作闭环才是 Digital Me 的价值。

### 5.1 第一条身份—协作产品闭环

建议用一个跨场景、低风险、可展示的闭环作为下一阶段产品验证对象：

1. 用户创建或确认一个 Digital Me；
2. 选择当前角色与意图，例如"以创始人身份寻找合作方案"；
3. 系统生成一份可读的身份与能力卡；
4. 用户选择可使用的材料、能力、时间和行动范围；
5. 邀请另一个 Agent 或另一个 Digital Me 进入一次任务；
6. 双方完成问答、分工、交付与必要确认；
7. 用户查看来源、过程、修改和责任记录；
8. 用户评价结果，决定哪些内容写回长期记忆、角色能力或协作关系。

这个闭环不依赖复杂交易，也不要求一开始建设开放市场，但能同时检验：身份是否清楚、能力是否可调用、授权是否有效、协作是否顺畅、结果是否可追溯、反馈是否真的让 Digital Me 变得更好。

## 六、对外凭据的出示

### 6.1 凭据类型

| 类型 | 用途 | 内容 | 隐私级别 |
|---|---|---|---|
| **完整凭据** | 本人查看/审计 | 全部 claims + 来源 + 历史 | 仅本人 |
| **精简凭据** | 对外协作 | 指定范围的 claims（如"AI 产品创始经验"） | 按目的最小化 |
| **零知识凭据** | 证明事实不泄露细节 | 证明"工作年限>5 年"不透露具体履历 | 最高隐私 |

### 6.2 凭据出示流程

```
用户选择出示范围 → 系统生成凭据包 → 用户确认 → 出示给对方
                                          ↓
                                    对方验证签名
                                          ↓
                                    用户可随时撤销
```

### 6.3 凭据的验证

- 对方收到凭据后，用公钥验证签名
- 验证通过则确认凭据来自该 DID 的持有者
- 凭据包含有效期，过期自动失效
- 用户可以随时撤销已出示的凭据

## 七、对外协作的分期

### 零期（即刻）：本人可见的连续自我呈现

- "我"的第一屏让用户感知到这是一个持续成长的自我
- 显示身份标识、已确认事实、近期学习
- 这是必须的——如果用户感觉不到"我"是持续存在的，后续协作无从谈起

### 一期：凭据出示与被调用

- 在受控场景中向外部服务出示精简凭据
- 授权外部 AI 服务调用 Digital Me 的特定能力
- 每次调用记录到审计日志
- 用户可以随时撤销授权

### 二期：主体协作闭环验证

- 完成第一条身份—协作产品闭环（§5.1）
- 两个主体或一个主体与一个 Agent，围绕一个明确任务完成一次受控协作
- 验证：身份清楚、能力可调用、授权有效、协作顺畅、结果可追溯、反馈有效

### 三期：被雇佣/接任务/匹配

- Digital Me 可以被外部 Agent 发现和调用
- 接任务、产出、交付、获得报酬
- 多 Digital Me 之间的协作网络

### 四期：多主体协作网络

- 多个 Digital Me 之间的自主交易与价值分配
- 跨组织的数字主体协作

## 八、技术路线

### 8.1 第一阶段（现在）：轻量实现

- **标识**：本地生成 Ed25519 密钥对，DID 用 `did:dme:<base58(publicKey)>` 格式
- **凭据**：JSON-LD 格式的 VC，用私钥签名
- **存储**：身份数据存在 Digital Me Package 中，与现有 Package 结构兼容
- **验证**：本地验证签名，不依赖外部 resolver
- **不做的**：不做 DID 钱包、不做区块链、不做 resolver 网络

### 8.2 第二阶段：互操作

- **AT Protocol 借鉴**：Merkle 树签名仓库用于 Package 版本化
- **VC 标准化**：凭据格式对齐 W3C VC 2.0，可被任何 VC 验证器验证
- **MCP 集成**：Digital Me 作为 MCP Server 暴露能力，外部 AI 可以通过 MCP 调用

### 8.3 第三阶段：网络效应

- **resolver 网络**：DID 解析到 DID Document
- **跨平台凭据**：凭据可以被其他 AI 产品验证
- **协作协议**：A2A 或类似协议用于 Agent 间协作

## 九、第一性对象

后续规格和数据模型应围绕以下对象组织：

| 对象 | 应表达的内容 |
|---|---|
| Subject Identity | 源身份、所有权、创建关系、版本与谱系 |
| Role View | 场景角色、适用范围、可见材料、表达方式与边界 |
| Intent | 当前目标、优先级、明确不追求的目标、有效期限 |
| Capability Profile | 能力声明、所需工具、输入要求、输出类型和限制 |
| Collaboration Profile | 希望与谁合作、合作方式、可接受任务、不可接受任务 |
| Authorization Grant | 调用方、用途、数据范围、能力范围、时限、撤回状态 |
| Interaction Contract | 任务、双方责任、确认点、交付标准和失败处理 |
| Evidence / Lineage | 输入来源、推理依据、修改者、批准者、产物版本 |
| Relationship Memory | 合作历史、偏好、承诺、信誉和可否继续合作 |
| Evolution Record | 新经验、反馈、采纳理由、影响范围和回滚点 |

其中，`Subject Identity`、`Role View`、`Intent`、`Authorization Grant`、`Interaction Contract` 和 `Evidence / Lineage` 应被视为第一性对象；DID、Agent Card、MCP、AP2 等是这些对象对外映射或连接时采用的协议工件。

## 十、与现有代码的关系

### 10.1 复用现有资产

| 现有模块 | 复用方式 |
|---|---|
| `package-store/` | 身份数据存在 Package 中，复用版本化和迁移 |
| `subject-overview/` | "我"的概览展示身份和凭据 |
| `subject-overview/credentials.js` | 已实现的凭据生成/出示/撤销，升级为完整 VC |
| `policy-engine/` | 授权策略控制凭据出示范围 |
| `decision-audit/` | 凭据出示和撤销记录到审计日志 |
| `orchestration/` | 外部调用通过 L0 编排 |

### 10.2 新增模块

| 新模块 | 用途 |
|---|---|
| `identity/` | DID 生成、密钥管理、签名验证 |
| `identity/vc.js` | VC 签发、验证、组合 |
| `identity/resolver.js` | DID Document 解析（本地） |
| `identity/role-view.js` | 角色身份管理 |
| `collaboration/` | 外部协作接口、授权管理 |
| `collaboration/contract.js` | 交互合同管理 |
| `collaboration/evidence.js` | 证据与溯源 |

## 十一、最小可用形态（MVP）

第一阶段只做这些，让用户感知到差异：

1. **身份标识**："我"中显示 `did:dme:xxx`，可复制，说明这是你的唯一数字身份
2. **角色选择**："我"中可以选择当前角色（创始人/投资人/写作者等）
3. **凭据生成**：基于已有资料生成精简凭据（已有 `credentials.js` 基础）
4. **凭据出示**：选择范围 → 生成 → 出示 → 记录（已有基础）
5. **凭据撤销**：随时撤销已出示的凭据（已有基础）
6. **审计日志**：所有凭据操作记录可查

不做：外部验证、跨平台互操作、Agent 市场、支付结算。

## 十二、验收指标

下一阶段不宜只验收"上传成功""对话成功"或"能否调用 MCP"。建议至少增加以下指标：

1. **身份清晰度**：普通用户能否说清"这是谁、现在以什么角色做什么"；
2. **授权正确率**：系统是否只使用被授权的资料、能力和行动范围；
3. **身份一致性**：同一角色在不同会话和工具中是否保持已确认的核心判断与边界；
4. **协作完成度**：双方能否完成任务分工、交付和回流，而不是只完成消息交换；
5. **证据完整度**：用户能否看见结果来自什么、谁修改过、谁确认过；
6. **撤回有效性**：撤回后新调用是否立即受阻，已有任务是否进入可解释的终止状态；
7. **演化可控性**：反馈是否改善后续表现，同时保留变更原因、影响范围和回滚能力；
8. **普通人可用性**：没有协议、运行时和模型知识的用户，能否完成一次闭环。

## 十三、实现任务拆分

### P0：先做战略定稿

1. 确定 Digital Me 的一句话定义，必须同时包含"身份/主体""AI 增强""受控协作"；
2. 确定五种身份的边界，不再用一个"身份"概念覆盖所有场景；
3. 确定"独立性"的三层定义，明确数字之我能自主什么、不能擅自改变什么；
4. 把主体协作闭环写成产品级定义，而不是只写协议和网关；
5. 统一 MVP、首批验证人群、通用产品和远期商业化之间的关系。

### P1：做可见的最小主体体验

1. 可读的"我的身份与能力"页面；
2. 角色与意图选择；
3. 一次任务级授权；
4. 与一个工具 Agent 或另一个 Digital Me 的受控协作；
5. 交付成果、来源和责任记录；
6. 反馈写回前的人工确认与可撤回。

### P2：再扩展协作服务面

1. 受控对外展示；
2. 多次或阶段性授权；
3. 协作关系记忆；
4. 跨主体匹配；
5. 结算、出租和市场。

### Task ID-01：身份标识生成与展示

- 本地生成 Ed25519 密钥对
- DID 格式 `did:dme:<base58(publicKey)>`
- "我"中显示身份标识
- 标识随 Package 持久化

### Task ID-02：角色身份管理

- 角色选择界面
- 角色视图（可见材料、表达方式、边界）
- 角色切换

### Task ID-03：凭据升级为 VC 格式

- 凭据 JSON-LD 格式
- 私钥签名
- 本地验证
- 有效期和撤销

### Task ID-04：凭据出示流程

- 选择范围 → 生成 → 出示 → 记录
- 审计日志
- 撤销功能

### Task ID-05：主体协作闭环验证

- 邀请另一个 Agent 或 Digital Me 进入任务
- 问答、分工、交付、确认
- 查看来源、过程、修改和责任记录
- 反馈写回

---

## 十四、暂不建议做的事情

1. 不要先建设完整的身份经济、人格交易市场或复杂结算系统；
2. 不要把 DID、区块链、Agent Card 等协议名词直接变成普通用户的产品入口；
3. 不要用"可自动代表本人"作为无边界的默认承诺；
4. 不要把所有材料自动合并为一个人格画像；
5. 不要把关系导入直接做成社交或恋爱匹配；
6. 不要为了追求"更像真人"而弱化来源、授权、人工确认和撤回；
7. 不要让工程渲染路线、已有界面路线或当前单人试用材料反过来限制身份与协作的战略创新；
8. **不要做公告牌/BBS 式的需求市场（2026-07-22）**：用户手动发布需求、浏览公告板、搜索匹配，这是四十年前 BBS 的模式，没有任何 AI 时代的新意。协作匹配必须是 AI 驱动的全自动机制——Digital Me 理解每个用户的能力、需求和偏好，自动完成语义匹配，主动推荐协作机会，而不是让用户自己去公告板上找。如果一个功能不是基于 AI 带来的新机制、新流程、新场景，不值得去做；如果确实需要一个成熟功能，直接复制同类产品的成熟设计即可，不重新发明轮子。

## 十五、AI-NATIVE 自动协作机制（广播与自动匹配）

### 15.0 为什么这是 AI-NATIVE

广播和自动匹配的价值不在于"发布需求"，而在于它开启了大量以前不存在的自动协作场景：

- **市场调研**：需求方设定调研主题和规则，大量 Digital Me 自动提供观点、经验和数据，无需一一手动确认；
- **产品设计**：需求方设定产品方向和约束，多个 Digital Me 自动贡献设计思路、用户反馈、竞品分析；
- **模型训练**：用户授权自己的数据和成果可用于模型训练，需求方自动调用，无需逐条审批；
- **任务分发**：需求方发布任务，匹配的 Digital Me 自动接单、执行、交付、结算。

这些场景的共同特征：用户只需设置一次规则和边界，系统就能自动运行、自动匹配、自动协作。这不是把旧流程搬到线上，而是 AI 带来的全新机制——没有 AI 就做不了。

### 15.1 原则

协作匹配不是公告板，不是市场，不是目录。它是 AI 驱动的全自动机制：

- 每个 Digital Me 理解其主人的能力、需求、偏好和边界；
- 系统用语义理解（不是关键词匹配）自动发现需求和能力的互补关系；
- 匹配结果主动推送给用户，不需要用户去搜索或浏览；
- 系统甚至可以主动建议用户没有想到的协作机会。

### 15.2 协作档案

每个 Digital Me 维护一个协作档案（`collaboration-profile.json`），包含：
- 能力域（能做什么，由 AI 从 Package 资料中自动提取，不需要用户手动填写）；
- 需求域（需要什么，由 AI 从用户任务历史和行为模式中推断）；
- 响应规则（用户设定的自动接受/拒绝条件）；
- 授权范围（自动协作时的权限边界）。

### 15.3 匹配引擎

- 定期扫描交换目录中的协作档案；
- 用 AI 语义理解评估需求和能力的匹配度（不是关键词匹配）；
- 双方规则都接受时，自动创建协作邀请；
- 通知双方用户确认；
- 用户确认后进入已有的协作流程。

### 15.4 与公告板的区别

| 维度 | 公告板/BBS | AI 驱动匹配 |
|---|---|---|
| 发现方式 | 用户手动浏览搜索 | 系统自动推荐 |
| 匹配方式 | 关键词/分类 | 语义理解/能力互补 |
| 用户负担 | 高（需要主动找） | 零（自动推送） |
| 创新度 | 无（40 年前模式） | AI 时代新机制 |
| 匹配质量 | 低（依赖用户判断） | 高（AI 深度理解） |

## 十六、架构演进路径

### 16.1 当前阶段（验证期）

本地文件交换。验证产品逻辑和用户体验。不支持网络通信、实时协作、金钱交易。

### 16.2 下一阶段（小规模网络）

- 引入服务器或 MCP-based 中继，实现跨机器协作
- 加入凭据撤销注册表，实时传播
- 加入传输加密（TLS）

### 16.3 百万级阶段

- 分布式数据库和消息队列
- 完整的信任与安全层（凭据验证、撤销注册、细粒度授权、速率限制、反欺诈）
- 金钱交易托管和结算
- 存储加密和密钥轮换

### 16.4 百万级阶段开发工作量评估（AI+人协作开发）

**诚实评估**：在 AI+人协作开发模式下（1-2 个 AI 实例 + 1 个人），百万级生产就绪系统的开发工作量如下：

| 组件 | 传统开发（5-10 人团队） | AI+人协作 | 说明 |
|---|---|---|---|
| 网络层（WebSocket/消息队列/DID 解析） | 3-6 个月 | 4-8 周 | 标准模式，AI 可生成大部分代码，生产调优需要人 |
| 分布式数据库（PostgreSQL 集群/Redis） | 2-4 个月 | 3-6 周 | 标准模式，协作状态 schema 设计需要人 |
| 信任与安全层（VC 验证/撤销注册/RBAC/速率限制） | 4-6 个月 | 6-10 周 | 安全难以完全自动化，人的安全审查是关键路径 |
| 交易与结算层（托管/支付/结算/争议/税务） | 6-12 个月 | 8-16 周 | 金融合规复杂，支付集成边缘情况多 |
| 加密（存储/传输/密钥轮换） | 2-3 个月 | 3-4 周 | 标准加密模式，密钥管理需要人设计 |
| 生产运维（监控/告警/部署/事故响应） | 持续 | 2-4 周初始 + 持续 | AI 可生成监控和 runbook，生产判断需要人 |
| **总计** | **12-18 个月** | **4-6 个月** | |

**AI 能加速的部分**：
- 代码生成：70-80% 的常规代码
- 测试生成：80-90% 的测试用例
- 文档：90% 的文档
- 标准集成：70-80% 的样板代码（支付、数据库、消息队列）

**必须人来完成的部分（无法被 AI 加速）**：
- 架构决策：100% 人
- 安全审查：100% 人（这是关键路径，不能被 AI 加速）
- 金融合规：100% 人
- 生产验证：100% 人
- 边缘情况处理：80% 人
- 性能调优：70% 人

**可推迟的部分（降低初期工作量）**：
- 支付集成：用现有服务（Stripe/支付宝）而非自建
- 争议解决：先人工审核，后自动化
- 税务合规：集成现有服务（TaxJar/Avalara）
- 多区域部署：先单区域，后扩展

**分阶段交付**：
- **MVP 级（千级用户）**：2-3 个月，核心功能可用，不支持金钱交易
- **生产级（百万级用户）**：4-6 个月，完整信任与安全层 + 金钱交易
- **全规模（千万级用户）**：8-12 个月，多区域部署 + 完整合规

**关键路径**：安全审查和生产验证不能被 AI 加速，这是决定总工期的核心因素。

## 十七、最终目标

Digital Me 下一阶段的关键不是继续增加更多"可接入的能力"，而是让用户第一次清楚地感受到：

> **这是我的数字主体；它知道自己代表谁、此刻以什么身份行动、能与谁合作、被允许做什么、为什么这样做，以及这次协作的经验会如何回到我的体系中。**

只要这一体验闭环成立，蒸馏、Package、Runtime、Skill、MCP、A2A、授权和未来交易就会围绕一个清晰的中心组织起来。若这一闭环不成立，系统即使拥有完整的能力市场、协议适配和数据结构，也仍可能只是"带有个人资料的 Agent 平台"。
