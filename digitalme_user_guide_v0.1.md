# Digital Me 用户手册（v0.1）

状态：持续加厚 · 与应用内「说明」同步  
最后更新：2026-07-15

> 面向普通用户：用场景语言说明「能做什么、先做什么、风险在哪」。技术协议名不出现在默认教学路径。

## 1. Digital Me 是什么

你的数字之我：在**像你本人**的约束下，协助对话、写作、研究、编程等任务；能力可增减，主体（是谁、授权、回流）始终由你掌控。

可记四层：

| 层 | 白话 |
|----|------|
| 主体编排 | 谁指挥、能否改文件、留下痕迹 |
| 场景 | 写文章 / 弄清问题 / 改代码 … |
| Skill | 用哪一种办事方式 |
| 工具能力 | 能读盘、搜网、连邮箱等手脚 |

## 2. 侧栏四处

1. **对话**：轻松问答。  
2. **工作台**：正式办事（按场景）。  
3. **我**：构建与查看数字之我。  
4. **能力**：管理 Skill 与工具能力。

每页顶部「说明」可打开对应手册章节。页面上的「?」提供短提示。

## 3. 工作台场景

### 3.1 写作

- 左侧：文稿库；中间：对话；右侧：正文。  
- 「采用为成果」把回复写入正文。

### 3.2 研究

- 左侧：课题；中间：对话；右侧：成果稿。  
- 有无参考材料会影响结论可信度标注。

### 3.3 编程（v0.3.13）

1. **任务备注**：便于你记忆的名字（可空）。  
2. **允许改动文件**：默认关＝只审阅；开启后才可改授权目录内文件。  
3. **执行体**：默认用本机对话与已启用工具；若在设置中配置了外部命令，可选用并在发送前确认。  
4. **行动记录**：左侧可查看近期授权与委派摘要；设置中也可查阅或清空。  
5. 中间对话在本列内滚动，输入框固定在底部。  
6. 「采用为成果」→ 右侧**成果台**：生成的文件、预览链接、说明摘要。  

写作与研究同样遵守「你确认授权、你看到轨迹、你采纳成果」；编程不强求界面上显式像你。

**成果不是只靠大段文字**：优先识别可打开的文件与链接；完整对话仍在中间可追溯。

**明确不做**：完整开发软件；无授权任意改全盘；不经确认调度外部命令；对外「雇佣市场」。

## 4. 能力页

- **Skill**：怎么办这件事；场景页仅用下拉引入。  
- **工具能力**：能调用什么；需密钥的在「高级」。  
- 引入 Skill 时会尝试准备其推荐工具。

## 5. 手册如何加厚（产品约定）

1. 每个新场景必须有：应用内「说明」+ 至少 3 个「?」短提示。  
2. 本文按场景追加章节，保持书面语严谨明白。  
3. 默认路径禁止堆协议名与开发黑话。

## 6. 还想深入

战略与拓扑见 `digitalme_context.md`、`digitalme_architecture_edge_sovereign_v0.1.md`；界面规格见 `digitalme_product_spec_v0.2.md`。
