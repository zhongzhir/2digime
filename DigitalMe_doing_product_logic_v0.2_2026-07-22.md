# Digital Me "做事" 产品逻辑 v0.2

日期：2026-07-22  
状态：`draft` — Owner 确认后进入实现  
上位依据：基线 v0.2 §2.4（能力覆盖完整性与 95 分位约束）

---

## 一、核心原则

**每个做事的能力要么独立达成 95 分位，要么通过与其它工具的协作达成同样目标。Digital Me 与其它工具的协作必须丝滑零成本。**

### 1.1 能力基线：个性化叠加，不得能力降级（2026-07-23）

“像我”不是把大模型变成能力更窄的专用模型。每个任务都必须以所接入大模型的通用能力为基线：

- 能力上限不因 Digital Me 的主体层而降低；
- 能力下限不应低于原始模型；
- 个人价值观、经验、风格和边界通过生成前叠加或生成后校对发挥作用；
- 如果接入 Digital Me 后结果变差，优先判定为编排、检索、复核或交互设计缺陷。

因此，Digital Me 的个性化增益应当是“通用 AI 能力 + 用户主体性”，而不是“通用 AI 能力 - 不像用户的内容”。

## 二、能力分类与达成路径

| 任务类型 | 95 分位达成路径 | Digital Me 的角色 |
|---|---|---|
| **写作** | **独立达成** | 生成初稿 → 编辑 → 导出。AI 写作已达到 95 分位，Digital Me 的个性化增益是差异层。 |
| **研究** | **独立达成** | 检索 + 分析 + 来源整理。AI 研究已达到 95 分位，Digital Me 的个性化增益是差异层。 |
| **文件处理** | **独立达成** | 解析 + 转换 + 生成。文件处理已达到 95 分位。 |
| **编程** | **协作达成** | Digital Me 不做 IDE。轻量编程（脚本、一次性代码）在 Digital Me 内生成初稿；日常开发通过协作达成——Digital Me 作为个性化上下文层被调用到程序员已有的开发环境（Cursor/VS Code/终端）。 |
| **视频/音频** | **协作达成** | 依赖外部工具（剪映/Descript等），Digital Me 提供脚本和创意方向。 |
| **邮件/日程** | **协作达成** | 依赖外部工具（邮件客户端/日历），Digital Me 起草内容，用户确认后发送。 |

## 三、"做事"的统一入口

**用户只需要理解：描述目标 → 得到结果。不需要选择任务类型。**

系统自动判断任务类型，路由到合适的达成路径：
- 独立达成的任务（写作/研究/文件处理）：Digital Me 内部完成
- 协作达成的任务（编程/视频/邮件）：Digital Me 生成可交付物，丝滑传递到外部工具

## 四、编程任务的正确形态

### 4.1 轻量编程（在 Digital Me 内完成）

- 写脚本、一次性代码、代码片段
- 用户描述需求 → Digital Me 生成代码 → 用户复制到自己的环境
- 这是"帮我写个工具"级别的需求，不是日常开发

### 4.2 日常开发（协作达成）

程序员不会离开自己的开发环境。Digital Me 的正确形态：

1. **作为 MCP Server**：Digital Me 暴露个性化上下文（技术判断、项目背景、代码风格、历史决策），Cursor/VS Code 通过 MCP 调用
2. **作为 CLI 工具**：程序员在终端里调用 `dm` 命令，Digital Me 提供上下文和辅助
3. **作为编辑器插件**：在 VS Code/Cursor 里安装插件，Digital Me 在侧边栏提供个性化辅助

**程序员的工作流不变**：打开自己的 IDE → 写代码 → Digital Me 在后台提供个性化上下文和辅助。

### 4.3 协作的关键指标

- **零成本接入**：程序员不需要改变工作习惯，不需要离开自己的环境
- **个性化增益**：Digital Me 提供的上下文让 AI 辅助更懂程序员（技术判断、项目背景、代码风格）
- **可验证**：程序员能感知到 Digital Me 的参与让结果更好，而不是增加负担

## 五、"做事"的界面布局

### 5.1 统一任务界面

```
任务标题（可选）
任务目标（必填）
[直接生成初稿]

高级：查看/调整本次使用的本人信息 ▸  ← 默认折叠

┌─────────────────────────┐
│      初稿内容显示区        │  ← 成果是第一视觉焦点
│      （可编辑）            │
└─────────────────────────┘

[保存修改] [采用] [否定] [查看依据] [导出]
```

### 5.2 协作达成的任务的界面差异

对于协作达成的任务（如编程），初稿显示区变为：

```
┌─────────────────────────┐
│      代码初稿显示区        │
│      （可复制）            │
└─────────────────────────┘

[复制代码] [在终端中使用] [查看依据]
```

- "复制代码"：一键复制到剪贴板
- "在终端中使用"：显示如何在自己的环境中使用（如 `dm run script.py`）

## 六、历史任务

- 显示所有任务（任何类型）
- 点击进入详情页，恢复完整状态
- 可以继续修改、重新生成、输出

## 七、左侧导航

```
对话    ← 快速问答、轻任务
做事    ← 统一任务执行环境（原"工作台"改名）
我      ← 数字自我、身份、凭据
能力    ← 能力管理（二层）
```

## 八、实现优先级

### 第一阶段（现在）

1. 统一"做事"入口，移除任务类型选择
2. 写作/研究/文件处理在 Digital Me 内独立完成
3. 编程任务生成代码初稿 + 复制功能
4. 历史任务恢复和继续

### 第二阶段

1. Digital Me 作为 MCP Server 暴露个性化上下文
2. 编程任务支持"在终端中使用"
3. 邮件任务支持"起草 → 确认 → 发送"

### 第三阶段

1. 编辑器插件（VS Code/Cursor）
2. CLI 工具
3. 视频/音频协作

## 九、验收标准

1. [ ] 写作任务在 Digital Me 内完成，达到 95 分位
2. [ ] 研究任务在 Digital Me 内完成，达到 95 分位
3. [ ] 编程任务生成代码初稿，可复制使用
4. [ ] 编程任务支持协作路径（MCP/终端），不改变程序员工作习惯
5. [ ] 所有任务使用统一入口，不需要选择任务类型
6. [ ] 历史任务可以恢复和继续
