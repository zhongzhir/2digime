# PAN-01S.1 主体解释与渐进式构建修订任务包

版本：v0.1  
日期：2026-07-19  
状态：`accepted`（2026-07-20；Owner real Electron runtime；baseline `cbde807fd1e40472d66fbe8f0810a0835e8bc816`）  
所属总任务：`P1-PANORAMA`（v0.4）  
前置实现：`98fb817eb4d177635497ca2a06c1624fe9b214cc`（PAN-01S）  
规格接受：`686fd7b1ee9a6f7e08f8221889fd67d717a8c4a9`（`docs(plan): accept PAN-01S.1 subject clarity revision`）  
前置状态（历史）：PAN-01S 曾为 `statically_verified` / `owner_changes_requested`；现与本任务一并 `accepted`  
实现分支：`codex/pan-01s-minimal-product-surface`（parent `686fd7b`）→ 收口延续 `codex/pan-01s2-chat-incident-close`  
代码 Owner：Cursor  

> **状态语义**
>
> - 本文件为 Owner 对 PAN-01S 验收未通过后的修订任务包；
> - 工程曾达 `statically_verified` / `implemented`；
> - **`accepted`（2026-07-20）**：Owner 真实 Electron 运行验收通过；acceptance basis = Owner real Electron runtime；baseline = `cbde807`；
> - **不得**开始 PAN-02（仍为 `planned` / `blocked`）；非阻断技术债进入 renderer foundation backlog，不重新打开本任务。

角色：Owner（产品验收）＋ Codex（规格与复核）＋ Cursor（本地实现）

## 1. Owner 验收结论

PAN-01S 的视觉减法方向正确，但产品表达仍未通过：

1. 进入“我”后，用户仍不容易理解“这是谁、它目前对我了解到了什么、我现在为什么要做这件事”。
2. “构建”仍是常驻管理后台，要求用户理解多个模块和完整流程，而不是引导用户一次完成一个动作。
3. 自我评测、两类材料建议、已授权文件夹等阶段性或管理性信息长期占据构建首页。
4. PAN-01R 生产入口不可见，帮助迁移和次级信息可发现性通过。

因此 PAN-01S 不得标记 `accepted`。本轮修订命名为 **PAN-01S.1**，只关闭上述产品表达缺口，不启动 PAN-02。

## 2. 本轮产品目标

用户进入“我”后，不需要理解 Digital Me 的工程结构，即可在几秒内知道：

- 这是基于我的经历、判断和边界持续形成的数字之我；
- 它目前对我了解到了什么程度，或者当前遇到了什么影响；
- 现在最值得做的一件事是什么。

用户进入构建过程后：

- 一次只看到一个当前任务；
- 可以选择低负担的开始方式；
- 完成一次构建后回到“我”，看到本次带来的变化；
- 阶段性建议完成后退出主路径；
- 管理性能力后移，但不删除、不破坏。

## 3. 冻结原则

1. **主体优先**：默认“我”呈现当前的 Digital Me，不呈现产品模块目录。
2. **结果语言**：说明“我目前知道什么、还缺什么、发生了什么”，避免“主体构成、版本健康、能力边界”等系统语言。
3. **单一下一步**：默认表面只有一个主操作，最多一个次操作和一条必要提醒。
4. **渐进式构建**：构建是一段可继续的过程，不是用户长期维护的控制台。
5. **用后退出**：自我评测、材料准备建议等完成后不再常驻。
6. **使用中成长**：首次构建只形成最小可用主体；后续理解主要发生在真实对话和做事过程中。
7. **复杂度后移**：数据来源、文件夹授权、历史批次、重做评测等进入现有次级入口；若不存在合适入口，本轮可以折叠在“更多方式”中，不得扩大为设置中心重构。
8. **安全骨架不变**：主进程可信派生、renderer 白名单、preview → confirmation → commit、Package 不变性和 test-only 隔离继续有效。

## 4. 默认“我”页面修订

### 4.1 页面结构

默认表面仍严格限制为：

```text
[主体名称]
[最多两行自然说明]
[一个主操作] [最多一个次操作]
[必要时一条简短提醒]
```

不得恢复统计墙、承诺墙、成长路线、PAN-01R CTA、工程证明条或常驻状态卡。

### 4.2 两行说明的语义

两行说明必须共同回答：

1. 这是什么／这是谁；
2. 它目前处于什么可理解的状态。

第一行使用稳定的主体解释：

> 这是基于你的经历、判断和边界，持续形成的数字之我。

第二行由可信状态派生，使用用户语言，不展示内部枚举名。

### 4.3 建议文案矩阵

| 内部状态 | 第二行自然说明 | 主操作 | 次操作 | 提醒 |
|---|---|---|---|---|
| P0 读取异常 | 我已经形成了部分认识，但目前有些信息无法读取。 | 恢复我的信息 | 查看目前的我（仅安全可读时） | 不展示技术错误；进入后再给诊断详情 |
| P1 尚未形成 | 我还不够了解你，可以从已有资料或一次简短对话开始。 | 让我认识你 | 稍后再说／无 | 不显示“missing”“uninitialized” |
| P2 待确认 | 我形成了一些新的认识，需要你确认后才会成为“我”的一部分。 | 确认我的理解 | 查看目前的我 | 不显示 inbox、queue、Package |
| P3 可继续完善 | 我已经有了基本轮廓，还有一项内容可以继续完善。 | 继续完善我 | 查看目前的我 | processing 可单独显示简短提醒 |
| P4 正常可用 | 我已经了解你的部分经历、关注领域和判断方式，并会在使用中继续成长。 | 开始一次对话 | 查看目前的我 | 仅必要时显示后台处理中 |

如产品实际数据不足以安全声称“经历、关注领域和判断方式”，P4 第二行应降级为：

> 我已经形成了初步认识，并会在对话和做事中继续了解你。

### 4.4 行为约束

- 保留现有 P0 → P4 求值顺序和可信主进程派生。
- 可以修改面向用户的 `Label` 和说明，但不得把状态重新交给 renderer 推导。
- `恢复我的信息` 可继续映射现有 `view_problems` action，不因文案变化新增不必要的 action。
- “查看目前的我”只有在存在安全可读内容时才出现；若整体不可读则不显示。
- 默认进入“我”时不得自动进入构建、审阅或问题页。
- 不得因本轮文案改动重新暴露 PAN-01R。

### 4.5 导航简化

- “我”页面应直接呈现主体，不再让“构建／数字之我”看起来像两个长期并列的产品模式。
- 如果现有 DOM 和路由不允许安全移除，可保留内部路由，但默认视觉中将“构建”降为当前主操作进入的流程，不作为常驻突出 tab。
- “全貌／认知／时间线／角色／观念与表达／边界”继续作为“查看目前的我”之后的次级浏览结构，不与默认主体说明争夺注意力。

## 5. 构建页改为状态驱动向导

### 5.1 不再采用常驻控制台结构

默认构建表面不得同时常驻展示：

- 待处理材料总面板；
- 完整三阶段按钮组；
- “建议先备齐两类材料”大卡；
- 可读文件夹列表；
- 自我评测模块；
- 已写入批次详情。

这些能力可以继续存在，但只能在当前步骤需要时显示，或从“更多方式／数据来源与授权／历史记录”进入。

### 5.2 向导状态

#### B0：尚未开始

```text
让我先对你有一个基本了解
你可以导入已有资料，也可以通过几分钟对话开始，不必事先整理完整档案。

[导入已有资料]
和我聊几分钟
稍后再说
```

- 主操作：导入已有资料。
- 次操作：和我聊几分钟；只有现有对话通道能够承接且不会偷跑 PAN-02 时才启用。
- 若本阶段尚无安全可用的引导对话能力，则显示为非阻断说明或暂不提供，不得伪造完成能力。
- “稍后再说”返回默认“我”。

#### B1：选择资料

```text
添加一些你已有的资料
简历、项目材料、文章或决策记录都可以，不必特意整理。

[选择文件]
什么资料比较有帮助？
```

- “履历与经历”“自我评测或判断材料”仅放在可展开帮助中。
- 不要求用户先备齐两类材料。
- 保留现有文件类型、安全读取和授权边界。

#### B2：已有待处理材料

```text
这些资料已经准备好了
本次将处理 26 份资料，并先请你确认关键认识，再写入数字之我。

[开始理解]
查看或调整资料
```

- 数量来自可信数据。
- 不在默认表面同时展示全部已写入文件和文件夹管理。
- 复用现有“开始构建”的确认与处理逻辑。

#### B3：处理中

```text
我正在理解这些资料
你可以先去使用其他功能，完成后我会提醒你确认。

[返回我的 Digital Me]
```

- 不要求用户停留在构建页。
- 不能承诺系统实际不具备的后台运行或通知能力；若 Electron 进程要求页面停留，应明确显示真实限制并进入 backlog，不能用假文案掩盖。

#### B4：待审阅／待确认

```text
我形成了一些新的认识
请确认哪些内容可以成为“我”的一部分。

[确认我的理解]
稍后处理
```

- 进入现有审阅机制。
- 保留事实、推断、观点和边界的写入规则。
- 不把确认简化为未经审阅的一键写入。

#### B5：本次完成

```text
这次认识已经完成
我新增或更新了：经历 × 项、观点 × 项、边界 × 项。

[看看现在的我]
继续补充
```

- 仅展示真实可计算的变化；无法可靠分类时使用“我已经根据本次确认更新了对你的认识”。
- 主操作返回默认“我”或对应主体浏览页。
- 完成态不是永久首页卡片；用户离开后构建入口回到下一项真实任务或低优先级“继续完善”。

### 5.3 状态转换

```text
B0 尚未开始
  → B1 选择资料
  → B2 待处理
  → B3 处理中
  → B4 待确认
  → B5 本次完成
  → 返回“我”
```

允许恢复中断：再次进入构建时直接回到当前真实状态，不从 B0 重来。

## 6. 自我评测的生命周期

自我评测是可选理解方式，不是构建页永久模块。

- 未完成且当前缺少判断／偏好材料时：可在“什么资料比较有帮助？”或“更多方式”中建议一次。
- 已完成：从构建主表面消失；结果保留在现有主体信息或来源记录中。
- 用户主动重做：从“更多方式”进入。
- 评测过期或实际使用暴露明显缺口：未来可情境化建议，本轮不开发新的智能触发器。
- 不得因隐藏常驻卡片而删除既有评测数据或破坏入口可达性。

## 7. 材料建议和可读文件夹

### 7.1 材料建议

- 删除“建议先备齐两类材料”的常驻大卡。
- 改为 B1 的一句低负担说明和一个可展开帮助。
- 已有足够待处理材料时不再显示建议。
- 同一文件可以包含多类内容，不要求用户先分类。

### 7.2 可读文件夹

- 从构建主路径移除常驻文件夹清单。
- 优先迁移到现有“设置／数据来源与授权”或等价次级位置。
- 若本轮没有合适的已有容器，放入构建页“更多方式”折叠区，默认关闭；不得为此大改设置中心。
- 添加、扫描、取消授权等现有能力和安全约束保持不变。

## 8. 明确非目标

PAN-01S.1 不包含：

- PAN-02 理解通道 Alpha；
- 新的对话蒸馏算法或自动访谈系统；
- prompt、Package 数据模型或检索机制重构；
- PAN-03 能力框架；
- PAN-04 外部协作；
- PAN-05 成长回流；
- 设置中心整体重构；
- 大规模视觉设计或 CSS 主题重做；
- 删除既有构建、评测、文件夹授权、审阅和确认能力；
- 用伪造进度、伪造后台任务或伪造统计满足界面文案。

## 9. 实现约束

1. 从当前 PAN-01S 实现 HEAD `98fb817` 开始，先确认已跟踪工作区干净。
2. 不 amend、不 squash、不 push；交接文件不纳入提交。
3. 不触碰 `digital-me-package/**`。
4. 不开始 PAN-02，不把尚不存在的对话理解能力伪装为可用。
5. 复用现有 action／navTarget 白名单；若必须增加目标，需先说明不可复用原因并补齐 fail-closed 测试。
6. renderer 不重新推导可信状态，不接收 query/hash/localStorage 伪造状态。
7. PAN-01R 继续保持生产不可发现、preload 不暴露、仅隔离测试 harness 可用。
8. 旧测试若绑定已失败的常驻构建 UI，应迁移到新的产品行为，不得为了旧断言保留失败界面。
9. 不对未跟踪交接文件执行删除、覆盖或纳入版本控制。

## 10. 必测场景

### 10.1 默认“我”

1. 用户能看到稳定主体解释＋当前状态说明。
2. P0 页面不再只显示“部分信息无法读取／查看问题”，而是说明影响并使用“恢复我的信息”。
3. P1、P2、P3、P4 分别显示用户可理解的主操作。
4. 默认进入“我”不被构建、审阅或 inbox 自动劫持。
5. 不出现 PAN 编号、Package、queue、内部状态枚举或错误堆栈。
6. 只有安全可读时才显示“查看目前的我”。

### 10.2 渐进式构建

1. B0 一次只呈现开始方式，不显示完整控制台。
2. B1 的材料说明不要求备齐两类材料。
3. B2 正确显示待处理数量并复用确认机制。
4. 中途离开再进入能恢复到真实当前状态。
5. 处理中不显示虚假的后台能力。
6. B4 仍执行审阅和确认，不直接写入。
7. B5 展示真实变化并能返回“我”。
8. 已完成自我评测不再常驻；仍可从次级入口访问或重做。
9. 已授权文件夹不再占据主流程；管理能力仍可达。
10. 构建完成后不长期保留完成卡片。

### 10.3 安全与回归

- PAN-01S 原有 P0～P4 优先级和冲突组合继续通过。
- renderer 不可伪造 `minimalSurface` 或构建状态。
- 生产环境无 PAN-01R UI、route、preload 或普通 IPC 入口。
- test-only harness 继续使用隔离 userData 与 hermetic fixture。
- 测试前后真实 Package 字节不变。
- 不泄漏绝对路径、敏感材料内容或内部错误详情到默认表面。
- 原 PAN-01S、PAN-01、PAN-01R、P1-03、P1-07、phase1 和 owner-runtime 最低回归继续全绿；根据新行为更新产品断言。
- 修改文件执行 `node --check`；最终执行 `git diff --check`。

## 11. Owner 验收问题（只使用产品语言）

实现完成后，Owner 只需判断：

1. 打开“我”，能否在几秒内明白这是自己的 Digital Me，以及它目前了解自己的程度？
2. 页面是否清楚给出现在最值得做的一件事，并能理解为什么？
3. 进入构建后，是否一次只要求完成一个动作，而不是管理一组模块？
4. 已完成评测或已提供材料后，相应提示是否退出主路径？
5. 中途离开再回来，是否能从真实进度继续？
6. 完成一次构建后，是否能看到“我发生了什么变化”并自然回到“我”？
7. 日常使用是否仍完全看不到 PAN-01R 体验入口和内部工程术语？

## 12. 提交与停止条件

建议：

- docs-only 规格接受提交：`docs(plan): accept PAN-01S.1 subject clarity revision`（本轮）
- 实现提交：`fix(ui): make subject identity and build flow understandable`

完成后提交报告应包括：

- 两个提交 hash、parent 和 message；
- 实际修改文件；
- 默认“我”五类状态的最终文案与去向；
- B0～B5 实际实现映射；
- 自我评测、材料建议、文件夹管理的新入口和消失条件；
- 所有测试结果、Loop 轮数、`git diff --check`、`git status`；
- 是否触碰 Package、是否存在 PAN-01R 生产入口、是否开始 PAN-02；
- 聚焦源码 zip、代码 diff、docs diff、stat、status，均不纳入提交。

实现完成后停止。不得开始 PAN-02，不得 push；等待 Codex 静态复核和 Owner 主路径验收。

### 12.1 规格接受记录（2026-07-19）

本任务包已接受进入实现：`specified` / `owner_approved_for_implementation` / `frozen_for_implementation` / `not_started`。规格接受提交 `686fd7b`。

### 12.2 实现记录（2026-07-19）

实现提交 parent = `686fd7b`；分支 `codex/pan-01s-minimal-product-surface`。状态曾为 → `statically_verified` / `implemented`（**不** accepted）。回归：`test:pan-01s`、`test:pan-01s-owner-runtime`、`test:pan-01-owner-runtime`、`test:p1-07-owner-runtime`、`test:owner-runtime`、`test:p1-phase1` 全绿。

### 12.3 Owner 真机验收 · accepted（2026-07-20）

- **状态：** `accepted`
- **Acceptance basis：** Owner real Electron runtime
- **Accepted baseline：** `cbde807fd1e40472d66fbe8f0810a0835e8bc816`
- **Accepted date：** 2026-07-20
- **一并 accepted：** PAN-01S、PAN-01S.2
- **下一门槛：** PAN-02 = `planned` / `blocked`；renderer foundation R0 = `planned` / `not_started`
- **未发生：** amend、squash、push；未启动 PAN-02；未开始 renderer 重构
