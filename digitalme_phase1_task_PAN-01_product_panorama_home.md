# PAN-01 任务包：产品全貌首页

版本：v0.2.3
日期：2026-07-19
状态：`statically_verified` / `owner_product_perception_failed` / `retained_as_scaffold`（不得标 `accepted`；不回滚；产品证明改由 PAN-01R 承接）
所属总任务：`P1-PANORAMA：Digital Me 产品全貌 Alpha`
前置任务：PAN-00 `accepted`（验收提交 `bc85a14`）
建议代码基线：`5ab55dc`
规划基线：`52b0d14`（PAN01_PLAN_BASE）
实现基线：`a40c5f8`（含 Codex 两轮复核修复）
实现分支：`codex/pan-01-product-panorama-home`
代码 Owner：Cursor
任务类型：Product Panorama Alpha / 只读产品纵向切片（scaffold）
后续任务：`PAN-01R`（`digitalme_phase1_task_PAN-01R_sovereign_collaboration_loop.md`）

---

## 0. 本任务结论

### 0.1 Owner 产品感知结论（2026-07-19）

PAN-01 工程上 `statically_verified`，但 Owner 产品感知验收**未通过**。统一裁定：

```text
engineering: statically_verified
owner: product_perception_failed
disposition: retained_as_scaffold
accepted: no
```

根因：展示并解释了承诺，但没有用一条真实体验证明承诺。「属于我」「由我控制」与「代表我协作」在当前页面主要仍是说明，而非可经历的证据。

**不回滚** PAN-01；保留为可信只读全貌 scaffold。非阻断 UI 细节进入 backlog。产品证明改由 **PAN-01R** 纵向主权协作闭环提供。PAN-02/03/04 在 PAN-01R 验收前暂停。

### 0.2 原任务目标（保留）

PAN-01 要把现有 Subject Home 从“主体数据与工程状态汇总页”升级为普通用户第一次进入“我”时即可理解的 **Digital Me 产品全貌首页**。

本任务只回答六个问题：

1. 这是哪个人的 Digital Me；
2. 它为什么属于本人；
3. 它依据什么理解本人；
4. 它目前能做什么、处于什么真实状态；
5. 哪些行动仍由本人控制；
6. 下一步如何继续构建、查看、增加能力和进入后续授权协作。

PAN-01 不建设完整控制中心，不建设协作运行时，不新增主体正式写入路径。它用现有可信只读聚合把完整产品故事第一次呈现出来。

---

## 1. 用户结果

### 1.1 一句话结果

> 用户点击“我”后，不需要先理解材料、Package、MCP 或内部数据结构，就能在首屏看见：这是我的 Digital Me、它由我掌握、它当前会什么、哪些仍需我授权、它将如何代表我协作。

### 1.2 首屏认知

在常用桌面窗口中，用户无需滚动或最多轻微滚动即可看见：

- 数字主体名称、所有者、私有状态和当前版本；
- “这是我 / 属于我 / 由我管 / 代表我协作”四个承诺；
- 构建我 → 看见我 → 武装我 → 授权我 → 代表我协作的成长路线；
- 每一步的真实五态；
- 一个清晰、真实、可执行的下一步。

详细数据构成、最近变化、能力、边界和警告放在首屏之后或渐进展开，不与四个承诺争夺第一视觉层级。

---

## 2. 产品结构决定

### 2.1 不新增第三套“我”页面

复用现有结构：

```text
我
├── 构建
└── 数字之我
    ├── 全貌（由现有“首页”升级并改名）
    ├── 认知
    ├── 时间线
    ├── 角色
    ├── 观念与表达
    └── 边界
```

不得再增加一个与现有 Subject Home 重复的独立导航层。

### 2.2 “我”的默认入口

用户从侧栏点击“我”时，默认进入：

```text
数字之我 → 全貌
```

不得因为投递箱存在待处理材料而自动切换到“构建”。待处理材料可以在全貌首页作为下一步提醒，但不能劫持主入口。

以下显式深链仍须保留：

- 用户主动点击“构建”；
- 构建流程主动聚焦审阅区；
- 其它页面明确调用 `goBuildView()`；
- 构建成功后进入“全貌”或指定的数字之我子页。

### 2.3 不新增正式写入

PAN-01 的主体数据路径是只读的：

- 不新增本人声明写入；
- 不新增发展意图正式写入；
- 不新增边界写入；
- 不迁移认知页零散编辑；
- 不绕开 PackageStore；
- 不修改 `digital-me-package/**`。

“我是谁 / 我要去哪里 / 哪些事不能替我做”在本任务中以摘要与已有入口呈现。尚无安全正式写入的内容必须显示“尚未建立”或“预览”，不得制造保存按钮。

---

## 3. 信息架构

### 3.1 第一层：数字主体 Hero

至少显示：

- `{主体名称}的 Digital Me`；名称未知时显示“尚未命名的 Digital Me”，不得编造姓名；
- 所有者：本人 / 已知显示名 / 未知；
- 当前仅本人可访问或隐私状态未知；
- 当前 Package revision；
- 最近变化摘要；
- 一句产品定位：属于本人、持续理解本人、只在本人授权范围内行动。

建议状态摘要：

```text
本机私有 · 资料由你保管 · 当前第 12 版 · 无自动外部授权
```

状态必须由主进程只读信息生成，不得在 renderer 静态写死“健康、私有、无授权”后冒充运行事实。

### 3.2 第二层：四个承诺

每张卡必须包含：承诺、当前五态、目前已有证据、一个真实入口或无入口说明。

#### 这是我

- 展示事实、本人声明、系统推断、当前状态、发展方向等分层摘要；
- 提供“依据什么理解我”入口，指向本页详细构成或现有认知页；
- 建议初始状态：`可用`，依据为 P1-03 Subject Home 已 accepted；
- 如果主体资料缺失或无法读取，必须降级，不得保持可用。

#### 属于我

- 展示本机资料、当前版本、是否存在可恢复版本；
- 提供“查看资料版本”入口；
- 建议初始状态：`实验`，因为本机与版本路径真实存在，但完整导出、迁移和跨端仍未完成；
- 不显示绝对路径，不返回 Package 正文。

#### 由我管

- 展示边界数量、当前外部自动授权状态和能力状态摘要；
- 提供“查看边界”入口；
- 建议初始状态：`实验`，因为边界、PolicyEngine 和部分停止能力存在，但完整控制权面板属于 PAN-02；
- 不提供尚未实现的“一键撤销所有授权”假按钮。

#### 代表我协作

- 明确“当前默认私有，无自动外部授权”；
- 说明本地协作沙盘将在 PAN-04 提供；
- 当前状态：`尚未开放`；
- 不提供可点击的假协作按钮，不把 contracts 示例文件显示为已开放协作。

### 3.3 第三层：五步成长路线

固定步骤：

```text
构建我 → 看见我 → 武装我 → 授权我 → 代表我协作
```

初始产品裁定：

| 步骤 | 用户面状态 | 依据与入口 |
|---|---|---|
| 构建我 | 实验 | Builder 主路径真实存在，但 P1-07 有冻结缺口；入口“继续构建” |
| 看见我 | 可用 | Subject Home 只读聚合已运行验收；入口“查看我的构成” |
| 武装我 | 实验 | 写作、研究、受控执行已有真实切片，验证范围有限；入口“查看能力” |
| 授权我 | 预览 | PolicyEngine 有真实底座，但 PAN-02 控制权产品面未完成；只说明下一阶段，不提供假控制按钮 |
| 代表我协作 | 尚未开放 | 无用户面协作闭环；PAN-04 才实现本地模拟 |

状态由主进程受控常量和真实运行信息共同派生。renderer 不得提交或覆盖状态。

如果 Package 缺失、主体读取失败或能力未配置，主进程应降低相关状态或增加独立“当前条件”说明，不得把连接状态与成熟度状态混成第六种用户面状态。

### 3.4 第四层：我的方向与核心边界

本阶段只读呈现：

- 本人确认的发展意图若存在，显示摘要；
- 如果只有模型整理出的观念、兴趣或能力信号，必须写成“发展方向线索”，不得冒充本人确认意图；
- 如果没有可证明的本人发展意图，显示“尚未建立本人确认的发展意图”；
- 边界至少显示启用数量和“查看边界”入口；
- 如展示边界文本，必须标明系统默认或本人设置，不得把系统默认冒充本人声明；
- 不在本任务新增编辑或保存。

如现有数据不足以可靠展示正文，允许只显示有解释的数量与状态，不允许从模型生成一段“我的目标”。

### 3.5 第五层：现有详细信息

保留并重新排序现有内容：

- 我的构成；
- 最近变化与可恢复；
- 我现在能做什么；
- 边界与对外状态；
- 需要注意。

“我的构成”应支持从“依据什么理解我”直接到达。七类分层仍须保留，事实、本人声明与系统推断不得混在一起。

---

## 4. 五态与能力映射

用户面只允许：

```text
可用 / 实验 / 本地模拟 / 预览 / 尚未开放
```

现有内部状态可以保留用于工程判断，但 renderer 必须显示主进程返回的 `userStatus` / `userStatusLabel`。

建议映射原则：

| 内部证据 | 用户成熟度 |
|---|---|
| 真实路径 + 对应运行验收 | 可用 |
| 真实路径，稳定性/边界/验证有限 | 实验 |
| 只在本机演示授权与协作 | 本地模拟 |
| 只能查看设计、草案或下一阶段说明 | 预览 |
| 没有可操作路径 | 尚未开放 |

“未配置智能引擎”“资料缺失”“当前连接失败”是运行条件，不创建新的成熟度标签。卡片可以同时显示：

```text
成熟度：实验
当前条件：尚未配置智能引擎
```

不得继续在 Panorama 首页显示“受限 / 实验中 / 不可用 / 未知”作为能力成熟度标签。

---

## 5. 主进程只读视图模型

### 5.1 优先扩展 SubjectOverview

优先以向后兼容的加法扩展现有 `SubjectOverview v1`，例如增加 `panorama` 字段；不要为同一页面复制一套平行读取体系。

建议结构可按实现裁剪：

```js
{
  contractVersion: "1",
  // existing identity/package/layers/...
  panorama: {
    statusContractVersion: "1",
    hero: { ... },
    promises: [ ... ],
    journey: [ ... ],
    direction: { ... },
    nextAction: { ... }
  }
}
```

强制要求：

- 五态和 `navTarget` 由主进程生成；
- renderer 不传入状态、所有权、授权数量或健康结论；
- `subject:getOverview` 继续不接受 renderer payload；
- 保持只读，调用前后 Package 字节不变；
- 不返回密钥、绝对路径、原始资料正文、persona 正文或完整边界文件；
- 不因 JSON 损坏或未知字段编造默认成功状态；
- 缺失或损坏时返回固定状态与脱敏 warning；
- 不把 contracts 示例文件视为真实协作授权。

### 5.2 能力状态

现有内部能力状态可保留，但新增用户状态字段，例如：

```js
{
  id,
  label,
  internalStatus,
  userStatus,
  userStatusLabel,
  currentCondition,
  limitation,
  navTarget
}
```

不得只在 renderer 把 `limited` 文案改成“实验”；映射必须可测试、可复用并由主进程控制。

### 5.3 导航目标白名单

只允许现有真实入口，例如：

- `me-build`；
- `me-overview`；
- `me-cognition`；
- `me-boundaries`；
- `capabilities`；
- `settings-package-versions`。

无真实入口的步骤不得返回可执行 `navTarget`。renderer 对未知目标 fail-closed，不执行任意 selector、脚本、路径或 IPC。

---

## 6. Renderer 行为

### 6.1 默认进入全貌

- 点击侧栏“我” → `数字之我` lane → `overview`；
- `overview` 子页签用户文案改为“全貌”；
- 不再以 inbox 是否有未写入材料决定默认 lane；
- 显式进入构建或审阅的调用链不受影响；
- 刷新全貌不切页、不改变 Package。

### 6.2 CTA

仅实现已存在的真实导航：

- 继续构建；
- 查看我的构成；
- 查看资料版本；
- 查看能力；
- 查看边界；
- 刷新全貌。

所有 CTA 使用普通用户语言。不得出现 PackageStore、MCP、IPC、contractVersion、dataKind 等内部术语。

PAN-02 / PAN-04 尚未实现的入口只显示状态与说明，不显示可点击假按钮。

### 6.3 失败与空状态

- 主体名称未知：显示“尚未命名的 Digital Me”；
- Package 不存在：显示资料未就绪和前往设置/构建的真实入口；
- 数据层计数未知：显示“尚无法确认”，不显示 0；
- 读取警告：用户可读、默认脱敏；
- 加载失败：保留页面骨架和重试入口，不输出绝对路径或异常堆栈；
- 无恢复版本：说明“尚无可恢复版本”，不显示成功绿色状态；
- 无外部授权：说明“无自动外部授权”，不声称已接入协作网络。

### 6.4 可发现性与可访问性

- Hero、四承诺和成长路线是首要视觉层级；
- 状态不能只靠颜色表达，必须有文字；
- 卡片和步骤保持键盘可达；
- 聚焦和滚动不应把用户意外带到构建、自评或页面中部；
- 常用桌面宽度下保持暖色、人感、克制；
- 窄窗口可纵向排列，不产生横向滚动；
- 不为本任务建立新的设计系统或动画体系。

---

## 7. 建议修改范围

允许根据实际代码裁剪，但原则上只涉及：

- `digitalme-app/src/subject-overview/index.js`；
- `digitalme-app/src/subject-overview/constants.js`；
- `digitalme-app/src/subject-overview/capabilities.js`；
- 必要时新增一个 `subject-overview/panorama.js`；
- `digitalme-app/src/main.js`（只允许只读聚合接线；优先不新增 IPC）；
- `digitalme-app/src/renderer/index.html`；
- `digitalme-app/src/renderer/app.js`；
- `digitalme-app/src/renderer/styles.css`；
- `digitalme-app/scripts/test-pan-01-product-panorama.cjs`；
- 必要的 PAN-01 Electron smoke test/harness；
- `digitalme-app/package.json`（只添加 PAN-01 测试脚本）；
- 本任务包、执行索引、能力表和 log 的状态更新。

若必须修改 `preload.js` 或新增 IPC，先在完成报告说明为什么现有 `subject:getOverview` 无法满足；新增 IPC 仍不得接受 renderer 提供的路径或状态。

---

## 8. 明确不包含

- PAN-02 完整控制权面板；
- PAN-03 能力页重构与三张能力卡的真实任务体验；
- PAN-04 本地协作沙盘；
- PAN-05 传播资产；
- P1-07 修复；
- Policies 全面迁移 PackageStore；
- 认知页零散编辑迁移；
- Life 读取体系重构；
- Package export/import；
- `package:load` scaffold；
- MCP 全面迁移 ToolBroker；
- 公网协作、Agent 发现、支付或结算；
- Digital Org 运行时；
- 新的主体正式写入；
- 大规模导航、设计系统或响应式重构；
- 修改 `digital-me-package/**`。

---

## 9. 测试要求

### 9.1 Hermetic 自动化

新增 `npm run test:pan-01`，只使用临时 fixture，不读取真实 Package。至少覆盖：

1. SubjectOverview 读取前后 Package 字节不变；
2. `panorama` 结构和状态契约固定；
3. 所有用户状态只来自五态；
4. renderer 无法传入或覆盖状态；
5. 这是我 / 属于我 / 由我管 / 代表我协作四卡存在且顺序稳定；
6. 五步成长路线存在且顺序稳定；
7. 初始状态裁定符合 §3.2、§3.3；
8. capability `internalStatus` 到 `userStatus` 映射 fail-closed；
9. 未配置智能引擎只改变 `currentCondition`，不生成第六种成熟度状态；
10. Package 缺失、JSON 损坏、名称未知时不编造身份、健康或可用状态；
11. payload 不含密钥、绝对路径、persona 正文、原始材料正文；
12. contracts 示例存在仍不得显示真实协作已开放；
13. navTarget 只来自白名单；
14. 未开放步骤无可执行 navTarget；
15. P1-03 原有测试继续通过。

### 9.2 Electron 主路径 smoke

新增或扩展真实 Electron smoke，至少验证：

1. 点击“我”默认显示 `数字之我 → 全貌`；
2. 即使 inbox 有待处理材料，也不自动进入构建；
3. Hero、四承诺和五步路线可见；
4. 页面不显示“受限 / 实验中 / 不可用 / 未知”作为成熟度标签；
5. “继续构建”进入构建；
6. “查看资料版本”打开真实版本入口；
7. “查看能力”进入能力页；
8. “查看边界”进入边界页；
9. 代表我协作显示“尚未开放”且没有假执行按钮；
10. 刷新后仍停留全貌；
11. 页面不依赖 Owner 真实 Package；
12. 不出现未处理异常、绝对路径或敏感日志。

建议脚本：

```text
test:pan-01
test:pan-01-owner-runtime
```

### 9.3 回归

至少执行：

- `npm run test:p1-03`；
- `npm run test:p1-07-owner-runtime`（确认显式审阅聚焦未被默认入口调整破坏）；
- `npm run test:p1-phase1`；
- `node --check`（所有修改的 JS/CJS）；
- `git diff --check`。

不得运行 `test:p1-baseline-real` 作为默认步骤，不得恢复或覆盖真实 Package。

---

## 10. Owner 验收主路径

Owner 使用临时资料目录验收：

1. 启动应用，点击“我”；
2. 确认默认进入“数字之我 → 全貌”；
3. 确认首屏能看见主体、四承诺、五步路线和下一步；
4. 用自己的话说明四个承诺；
5. 核对姓名、所有者、私有状态、版本是否真实；
6. 核对事实 / 本人声明 / 系统推断未混淆；
7. 点击“继续构建”，确认进入构建；
8. 返回全貌，点击资料版本、能力、边界入口；
9. 确认“授权我”为预览、“代表我协作”为尚未开放；
10. 确认没有协作假按钮或公网已连接暗示；
11. 放入一份待处理材料后重新点击“我”，确认仍回到全貌而不是构建；
12. 切回常规资料目录，确认未发生写入。

Owner 感知问题只修主路径不可发现、状态误导和结构阻断。普通间距、措辞偏好和非阻断视觉问题进入 backlog，不反复打磨。

---

## 11. 完成定义

以下全部满足，PAN-01 才可提交复核：

1. “我”的默认入口是产品全貌；
2. 首屏建立主体、归属、掌控、能力和协作方向的整体认知；
3. 四承诺和五步路线状态真实且只使用五态；
4. renderer 不能决定状态、所有权或授权事实；
5. 未完成的 PAN-02 / PAN-04 不出现假按钮；
6. 现有 SubjectOverview 七类分层和版本信息仍可访问；
7. 明确区分本人确认发展意图与系统方向线索；
8. 显式构建与审阅深链不被默认入口改变破坏；
9. hermetic 与 Electron 主路径测试通过；
10. P1 阶段回归通过；
11. 未触碰真实 Package；
12. Owner 完成一次主路径体验，Codex 完成范围与状态复核。

---

## 12. 开发节奏与 Loop 边界

PAN-01 允许使用 **有界 Loop**：

```text
实现 → 静态检查 → hermetic 测试 → Electron smoke
→ 仅修阻断问题 → 再验证 → 停止
```

限制：

- 最多两轮自动修复；
- 不自动修改本任务包或产品规格扩大范围；
- 不修无关历史问题；
- 不触碰真实 Package；
- 不自动 amend、push 或开始 PAN-02；
- 普通视觉与文案微调进入 backlog；
- 两轮后仍失败，报告阻断并停止；
- Owner 产品感知不能由测试替代。

---

## 13. 分支与提交建议

### 13.1 规划收尾提交

在 `bc85a14` 上先完成：

- PAN-00 状态更新为 `accepted`；
- 执行索引更新为 PAN-01 `specified`；
- 新增本任务包；
- 能力表和 log 记录 PAN-00 验收与 PAN-01 启动。

建议提交：

```text
docs(plan): accept PAN-00 and specify PAN-01
```

### 13.2 实现分支

从上述规划提交创建：

```text
codex/pan-01-product-panorama-home
```

实现提交建议：

```text
feat(ui): add Digital Me product panorama home
```

不得 amend PAN-00 提交，不得 push。

---

## 14. Cursor 完成报告

```markdown
## PAN-01 完成报告

### 分支与提交
- planning commit：
- branch：
- implementation commit：
- amend：否
- push：否

### 用户获得的新感知
- ...

### 真实 / 实验 / 预览 / 尚未开放
- ...

### 默认入口与导航
- ...

### 主进程状态契约
- ...

### 修改文件
- ...

### 测试
- test:pan-01：
- test:pan-01-owner-runtime：
- test:p1-03：
- test:p1-07-owner-runtime：
- test:p1-phase1：
- node --check / git diff --check：

### 边界
- 是否触碰 digital-me-package/**：
- 是否新增主体写入：
- 是否实现 PAN-02 / PAN-04：
- 是否纳入交接文件：

### 非阻断 backlog
- ...
```

提交完成报告后停止，不开始 PAN-02，不标 PAN-01 accepted。
