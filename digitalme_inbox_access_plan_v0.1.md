# Digital Me：材料投递箱 × 可读范围 — 构建产品规划 v0.1

版本：v0.1  
日期：2026-07-12  
状态：正式规划（指导「我 · 材料」与授权文件夹实现）  
关联：`digitalme_person_definition_v0.1.md`、`digitalme_product_spec_v0.2.md` §6.5、`digitalme_material_guidance_v0.1.md`、决策 #31–#32

---

## 0. 产品结论

**构建主路径**：用户不必先分类。  
「投材料 / 授权可读范围 → 系统归类抽取 → 人确认 → Package 成型」。

**算法角色**：
- 同一套材料，不同算法产出应**基本是同一人**；
- 差异主要在挖掘深度与应用深度，不是换人格；
- Package 是事实源；分类/蒸馏算法可替换、可重跑。

**可读范围硬约束**：
- 允许：用户明确授权的本机文件夹（及后续经能力授权的渠道）；
- 禁止默认：全盘扫描、静默读手机、未说明范围的「开放」；
- 话术：「你授权我看这些文件夹」，不说「访问你的电脑」。

---

## 1. 用户旅程

| 步骤 | 用户看到 | 系统做 |
|------|----------|--------|
| 投入 | 多选文件 / 添加可读文件夹后扫描 | 登记进投递箱（原始路径保留） |
| 整理 | 「正在阅读 n 份材料」 | 解析正文 → 建议管道 + 置信度 + 短理由 |
| 确认 | 可改归类：观念 / 事实 / 保管 / 待定 | **不**自动写入 Package |
| 处理 | 进入现有审阅勾选 | 走 persona / identity / custody 管道 |
| 之后 | 概览覆盖度上升；工作台更像我 | lifeSummary / RAG / 边界生效 |

---

## 2. 入口定义

### 2.1 材料投递箱（Inbox）

- 位置：「我」内 Tab **材料**
- 支持：docx / txt / md / pptx / pdf 多选入队
- 状态：`queued` → `suggested` → `processing` / `written` / `skipped`
- 首版：整文件一条建议；同文拆段后置

### 2.2 可读范围（Authorized Access）

- 授权本机文件夹 + 扩展名白名单 + 是否含子目录
- 「扫描新文件」仅**入投递箱待确认**，不静默写入
- 去重：路径 + 可选内容指纹
- 邮件/微信/手机：后置，仍落投递箱

---

## 3. 分流标签

| 标签 | materialKind | 保守规则 |
|------|--------------|----------|
| 观念与表达 | `persona` | 明显履历表 → 不标观念 |
| 人生事实 | `identity` | 无时间/角色信号 → 降置信或待定 |
| 仅保管 | `custody` | 证件/密码/银行卡/他人隐私 → 默认保管 |
| 待定 | （需用户选） | 置信度低 |

分类：规则特征 + 可选短模型调用；失败则待定。

---

## 4. 信息架构

```text
我
├── 概览     覆盖度 + 投递材料 / 可读范围入口
├── 材料     投递箱 + 可读范围管理   ← 主构建入口
├── 时间线   轴编辑；事实结果落点（专项导入可保留为捷径）
├── 角色
├── 观念与表达  问卷 + 高级文件导入
└── 边界
```

---

## 5. 分期 DoD

### B1 投递箱最小可用（本轮）

- [x] 「我 · 材料」入队、列表、状态
- [x] 「整理」：建议标签 + 理由 + 置信度
- [x] 改标签后「确认处理」→ 现有 distill 审阅写入
- [x] 本规划文档 + 规格升版

### C1 可读范围（本轮紧随）

- [x] 添加/移除授权文件夹
- [x] 手动扫描入投递箱（去重）
- [x] 授权说明文案清晰

### D+ 后置

邮件/微信能力、手机投递、同文拆段、启动时自动增量。

---

## 6. 决策

见 `digitalme_context.md` **#32**。
