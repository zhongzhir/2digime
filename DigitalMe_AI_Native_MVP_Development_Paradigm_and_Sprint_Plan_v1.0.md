# Digital Me AI 原生开发范式与 MVP 冲刺计划

版本：v1.0  
日期：2026-07-21  
状态：Owner 决策稿  
目标：以可运行代码和真实用户闭环为主体，在不妥协基本质量底线的前提下，尽快完成 Digital Me MVP

## 1. 纠偏结论

Digital Me 不应按传统团队的周/月排期推进。项目当前的核心资源是 Owner 可以即时决策，Codex 可以即时规划、审计和验收，Cursor 可以即时编码，三者能够在同一天完成“决定—实现—运行—修正”。此前按 12～16 周乃至 2027 年 3 月估算，错误地把每一个内部步骤都当成独立项目阶段，把治理过程当成了主要产出。

新的基准目标是：

- **今天完成 R2 对话与会话迁移**；
- **7 个有效开发日形成内部 MVP 主闭环**；
- **10 个有效开发日达到 Owner 可连续使用的 MVP**；
- **14 个有效开发日形成可交给少量种子用户试用的 MVP 候选版**。

“有效开发日”指 Owner、Codex、Cursor 至少完成一次完整开发循环的日期，不等于传统公司的工作日。若当天可以连续推进多个循环，不人为限制为一个任务。

以上是冲刺目标，不是以牺牲数据安全、授权边界或可恢复性换取的演示时间。若出现真实架构阻断，可调整具体日期，但不得自动退回以周为最小单位的流水线管理。

## 2. MVP 的唯一验收叙事

MVP 只证明一条完整而真实的纵向闭环：

> 一个自然人将材料交给 Digital Me，构建出可核对、可修改、可迁移的“我”；这个数字主体拥有稳定身份和能力说明；本人向它授予一项有边界的任务；它调用一种真实能力完成任务，形成产物；系统记录代表关系、授权、执行和结果；如涉及价值交换，形成报价、批准和结算凭证；结果回流到 Digital Me，但未经本人确认不得擅自改写核心自我。

如果这条链路在一台真实电脑上从头到尾跑通，MVP 成立。尚未服务百万用户、尚未接入区块链或真实支付、尚未形成能力市场，不影响 MVP 成立。

## 3. MVP 必做与暂缓

### 3.1 必做

| 领域 | MVP 必须达到的真实能力 |
|---|---|
| 对话与会话 | 新 renderer 下可创建、切换、改名、删除和恢复会话；附件正文不污染聊天展示或持久化；请求可停止；会话可靠保存 |
| 构建 | 选择材料 → 系统分流/蒸馏 → 用户核对修改 → 写入 Package → 对话可实际调用这些结果 |
| 数字之我 | 用户能看见“我是谁、知道什么、如何判断、如何表达、有哪些边界”，并能纠错 |
| 能力 | 至少一种真实能力可启用并用于任务；MVP 首选“研究/写作产物”，复用现有模型与工具，不建设通用能力市场 |
| 身份 | 为 Digital Me 生成稳定主体 ID 和本地密钥；能对主体声明、Package 摘要和行为凭证签名并验证 |
| 代表 | 每次外部或高后果行动明确显示“Digital Me 代表谁、以什么身份行动”，禁止冒充自然人 |
| 授权 | 单次任务授权单：授权人、被授权主体、动作、数据范围、能力、有效期、风险、费用上限、撤销状态；高风险动作本人确认 |
| 最小交易 | 报价/费用意图 → Owner 批准 → 执行 → 结算记录/凭证。允许模拟或人工结算，但状态和凭证必须真实，不伪称已支付 |
| 审计与回流 | 一条时间线能回答谁、何时、依据何种授权、调用什么能力、使用哪些资料、产生什么结果；结果可由用户确认后回流 |
| 主权 | Package 可导出；核心数据本地优先；授权可撤销；失败不毁坏既有数据 |

### 3.2 本轮明确暂缓

- 完整 W3C DID 网络发布、链上锚定和钱包产品；
- AP2/x402 的完整生产接入与真实资金自动收付；
- 多用户平台、能力市场、自动撮合、出租和接活市场；
- 云同步、移动端和百万用户扩容；
- 多模态全数据覆盖；
- 多职业场景墙和大量内置能力；
- 为“以后可能需要”而进行的大面积架构重写；
- 与 MVP 主闭环无关的文档统一、术语美化和历史记录整理。

暂缓不等于删除方向。MVP 的数据结构应保留标准映射点，但只实现当前闭环真正使用的部分。

## 4. 新开发范式：闭环驱动，而非工序驱动

### 4.1 产出排序

每个开发日的资源按以下顺序分配：

1. 可运行的产品代码；
2. 自动化测试与数据安全验证；
3. Owner 真机体验和阻断修复；
4. 为下一次编码所必需的简短合同；
5. 状态记录。

文档本身不再被视为里程碑，除非它直接解除编码歧义或控制高风险行为。

### 4.2 一日多循环

单个循环以 60～180 分钟为常见尺度：

1. **冻结本轮结果**：用 5～15 条可观察验收项说明用户最终能做什么；
2. **直接实现**：Cursor 连续编码，允许在已冻结边界内自行完成必要的内部重构和测试；
3. **自动检查**：类型、单元/集成测试、关键 E2E、构建；
4. **Codex 集中复核**：只审查行为、数据边界、回归和越权，不逐句雕琢状态文档；
5. **Owner 真机验收**：围绕真实任务使用；
6. **即时修复**：阻断问题当场回到第 2 步；通过后立即进入下一个闭环。

一天可以完成多个循环。不得因为“一个提交已经完成”而等待第二天。

### 4.3 授权从“逐步骤”改为“目标包”

Owner 一次授权一个完整目标包及其安全边界。例如“今天完成 R2”，授权范围覆盖为达成 R2 验收所需的源码、测试和小型内部重构。

只有以下情况必须停下再次请示：

- 需要改变用户已确认的产品行为；
- 需要读取或迁移真实 Package、sessions 或用户隐私数据；
- 需要不可逆数据变更；
- 需要外发消息、公开发布、真实付款或签约；
- 需要引入改变安全边界的外部依赖或云服务；
- 发现现有架构无法在合理范围内修复，必须扩大任务范围。

分支内正常编码、补测试、修复同一目标内缺陷、拆分或合并内部模块，不再逐项等待 Owner 授权。

### 4.4 风险分级

| 等级 | 范围 | 开发方式 |
|---|---|---|
| L0 低风险 | UI、文案、布局、非持久化交互 | 先实现，提交后集中审查 |
| L1 中风险 | 会话、Package 派生数据、能力调用、普通本地持久化 | 先写简短行为合同，当天实现并自动测试 |
| L2 高风险 | 密钥、授权、代表行为、隐私出域、交易、不可逆迁移 | 先做威胁清单和失败语义；实现后独立安全复核；高后果路径保持人工确认 |

L2 的严格性不得下放给所有普通页面；L0 的速度也不得上移到密钥和交易代码。

### 4.5 合同和文档瘦身

活跃开发期只维护三个权威对象：

- **产品验收清单**：用户能做什么；
- **实施合同**：数据边界、失败语义和本轮允许范围；
- **执行看板**：待做 / 进行中 / 已通过 / 阻断。

`context`、`log`、全景索引等历史文档改为每日结束或里程碑结束时批量同步一次。相同状态不得人工复制到七八个文件。若旧文档与当前执行看板冲突，以最新 Owner 决策和目标包为准，并在里程碑收口时统一修正。

### 4.6 架构原则：可修复，不求预先完美

MVP 架构必须具备：

- 主体层与能力层解耦；
- renderer 不成为 Package、会话或授权的第二事实源；
- main 持有文件、密钥、权限、持久化和外部调用权威；
- 重要记录版本化，Schema 有版本号和迁移入口；
- 写入可恢复，错误不以空数据覆盖正式数据；
- 身份、授权、交易以独立模块和稳定接口实现，后续可替换为 DID/AP2/x402 适配器；
- 外部能力通过统一 adapter/capability 接口接入。

允许以后修复的内容包括 UI 组织、内部命名、非关键数据结构、单机场景下的性能和临时 adapter。不得以后再补的内容包括密钥明文泄露、授权绕过、把完整附件写入聊天记录、伪造支付状态和无法恢复的数据覆盖。

## 5. 10 个有效开发日的 MVP 冲刺

以下“天”是目标包，不是必须串行等待的行政阶段；提前完成立即进入下一项。

| 日 | 目标包 | 当日必须形成的可运行结果 |
|---:|---|---|
| D0（今天） | R2 对话与会话 | 完成新规范下对话/会话迁移；真实运行；Owner 完成创建、发送、停止、切换、改名、删除、重启恢复和附件边界验收 |
| D1 | R3 我/构建表面 | 新 renderer 中“我”与“构建”可用；旧核心服务直接复用；材料进入、状态可见、错误可恢复 |
| D2 | 构建闭环 | 一份真实材料完成分流/蒸馏、用户核对、写入 Package；对话能够引用新写入的个人信息且有来源边界 |
| D3 | 能力闭环 | 至少一个真实任务能力从“启用”到“调用”到“产物落盘”跑通；推荐以研究/写作为首能力，不开发市场 |
| D4 | 主体身份 | 生成稳定 Digital Me ID、本地密钥、主体声明；完成签名与验证；UI 可查看但不暴露私钥 |
| D5 | 代表与授权 | 创建单次任务授权单；执行前展示代表关系、范围、有效期、数据和费用上限；支持拒绝/撤销；越权调用失败 |
| D6 | 最小交易 | 形成报价或费用意图、Owner 批准、任务履约、人工/模拟结算状态和签名凭证；明确区分“待支付/已人工确认/模拟” |
| D7 | 纵向总闭环 | 从构建一个“我”开始，到授权 Digital Me 调用能力完成付费/计费任务，再到结果回流和审计，连续跑通一次 |
| D8 | 破坏性测试与恢复 | 重点攻击附件泄漏、会话损坏、token 过期、越权、撤销后调用、签名篡改、重复执行、崩溃恢复；修复阻断项 |
| D9 | Owner 连续使用 | Owner 使用真实材料和真实任务连续工作；不按测试脚本走；修复影响主闭环的体验问题 |
| D10 | MVP 冻结 | 形成可重复演示包、最小用户说明、已知限制和恢复办法；打 MVP 内部标签/版本 |

目标日期：若从 2026-07-21 连续推进，**内部 MVP 目标为 2026-07-31 前后**。即使中间存在 2～4 个缓冲日，也应在 **2026-08-05 前**形成内部 MVP，而不是延伸到数月后。

## 6. 14 个有效开发日的种子用户候选版

D11～D14 只处理外部试用必要项：

- 新用户冷启动和示例材料；
- 密钥/Package 备份与恢复提示；
- 崩溃和错误信息可理解；
- 安装包与版本升级；
- 隐私、授权和模拟交易的明确声明；
- 3～5 名种子用户各完成一次主闭环；
- 只修复 P0/P1，普通优化进入后续列表。

种子用户候选版目标：**2026-08-10 前后**。是否对外发送由 Owner 根据内部 MVP 质量决定。

## 7. 身份、代表、授权、交易的最小技术切片

### 7.1 身份

MVP 不先做完整 DID 产品，而实现一个可升级的 `IdentityProvider` 接口：

- 本地生成 Ed25519（或项目现有密码学栈中同级成熟方案）密钥对；
- 私钥进入操作系统安全存储或受保护的本地密钥存储，绝不进入 renderer、Package 导出或日志；
- 主体 ID 由版本化方法标识 + 公钥指纹形成，稳定且可验证；
- 生成可读主体声明和机器可读主体文档；
- 签名覆盖规范化后的主体声明、授权单和交易凭证；
- 将来通过 adapter 映射为 `did:key`、其它 DID 方法或平台 Agent Card。

这里的“唯一”指在所采用命名/密钥体系中可稳定区分并可密码学验证，不宣称已解决全球法律身份唯一性，也不把自然人实名认证与数字主体 ID 混为一谈。

### 7.2 代表与授权

采用最小 `Mandate` 对象：

```text
mandateId
principalHumanId
digitalMeId
action
resourceScope
capabilityId
purpose
validFrom / expiresAt
riskLevel
spendingLimit
requiresHumanConfirmation
status: draft | active | consumed | revoked | expired
signature
```

每次执行由 main 校验：签名、状态、时间、主体、会话、能力、资源范围、费用和一次性/重复使用规则。renderer 只提交意图，不得自行判定授权有效。

### 7.3 最小交易

采用 `TransactionIntent + Receipt`，而不是先建支付平台：

- `TransactionIntent` 说明买什么、由谁提供、价格/计量、授权上限、支付方式和当前状态；
- Owner 明确批准后才允许执行；
- `Receipt` 记录授权、能力调用、产物哈希、金额、结算方式与状态；
- 模拟结算必须标记 `simulated`，人工线下结算标记 `manually_confirmed`，不得显示为平台已完成支付；
- 未来支付 adapter 可接入 AP2/x402/支付渠道，不改上层主体与授权语义。

## 8. 质量底线与测试策略

### 8.1 五条不可妥协底线

1. 不丢失或以空数据覆盖 Package、sessions 和授权记录；
2. 私钥、完整附件、真实路径和敏感正文不进入 renderer DTO、聊天 DOM、日志或非必要持久化；
3. 撤销、过期、越权和签名错误必须在外部调用前失败；
4. 外部行动、高风险决定和费用行为不得绕过明确的人类确认；
5. 系统不得把草稿说成已保存、把模拟说成真实、把未支付说成已支付、把 Digital Me 冒充为自然人。

### 8.2 测试金字塔

- 每次提交：类型检查 + 与修改相关的单元/集成测试；
- 每个目标包：关键路径 E2E + 构建；
- D7/D10：完整纵向闭环 E2E；
- L2 模块：负向测试优先于覆盖率数字，包括篡改、重放、过期、撤销、错主体、错会话、超额和失败恢复；
- Owner 真机验收用于发现产品问题，不替代自动测试。

不要求每个 UI 分支达到形式化的全覆盖，不以追求覆盖率数字拖延闭环。

## 9. 缺陷与返工规则

| 等级 | 含义 | 处理 |
|---|---|---|
| P0 | 数据损坏、隐私/密钥泄露、授权绕过、伪交易、应用无法启动 | 立即停止后续目标，当场修复 |
| P1 | 主闭环无法完成、结果错误、无法恢复、用户被困 | 当前目标包内修复，不带入下一日 |
| P2 | 有绕行路径的功能/体验缺陷 | 记录，D8～D10 集中处理 |
| P3 | 文案、视觉、非关键一致性 | MVP 后处理，除非严重误导授权或交易 |

同一缺陷最多允许两轮局部修补。两轮仍未解决，必须回到根因和边界重新设计，不继续叠加补丁。

## 10. 三方协作方式

### Owner

- 决定目标、取舍和真实体验是否成立；
- 一次授权完整目标包；
- 用真实材料、真实任务做高价值验收；
- 对高风险授权和交易语义作最终决定；
- 不承担内部实现细节审批。

### Codex

- 把目标翻译为最小可验收合同和 Cursor 指令；
- 主动删除非必要流程和范围；
- 审查架构、安全、数据边界和真实运行结果；
- 对普通实现问题给出明确修复方案，不把每个问题升级为 Owner 决策；
- 以代码和真实产品进展为主要评价对象，而非文档数量。

### Cursor

- 在目标包内连续实施、测试、构建和修复；
- 对范围内的内部技术选择拥有执行自主权；
- 发现越界或真实风险时停止并报告；
- 每个目标包交付一个或少量有意义提交，不为流程刻意拆成大量文档提交；
- 报告真实运行证据、失败项和未完成项，不以“代码已写”代替“产品可用”。

## 11. 每日执行看板模板

```text
今日唯一结果：
用户完成的真实闭环：
允许修改范围：
不可突破底线：
自动检查：
Owner 真机路径：
P0/P1：
未完成但不阻断项：
提交：
下一目标包：
```

每日不再生成长篇重复状态报告。只有发生数据风险、范围改变、架构阻断或 L2 安全问题时，才提供专项报告。

## 12. 立即生效的 R2 决策建议

不再只授权 R2-A 后停下来等待。建议 Owner 直接授权：

> 在 `codex/r2-chat-sessions-migration` 分支于今天连续完成 R2-A～R2-F，以 R2 完整用户闭环和冻结合同为边界。Cursor 可在范围内修改源码、测试和必要配置，连续执行测试、构建及应用验证；Codex 在实现完成后集中复核，Owner 当天真机验收。除发现 P0 风险、需要读取/迁移真实用户数据、不可逆变更或必须改变已冻结产品行为外，不得因内部子阶段、提交或普通参数再次等待授权。R2 完成前不得扩展到 R2.5、PAN-02 或无关功能，不得 push。

R2 当天通过后，直接进入 D1“我/构建”，不再新增“是否允许开始 R3”的空档。Owner 对本冲刺计划的批准，即构成对 D0～D3 中已定义范围的里程碑授权；D4～D6 身份、授权、交易因属 L2，在进入编码前由 Codex提交一页以内的威胁清单和失败语义，Owner一次确认后连续完成。

## 13. 完成定义

Digital Me MVP 的完成，不以文档版本、页面数量或协议名词为准，而以 Owner 能否在真实电脑上完成以下动作并相信结果为准：

1. 我能把自己的材料交给它；
2. 我能看见并纠正系统构建出的“我”；
3. 它在对话和任务中确实使用了“我的”信息、判断和边界；
4. 我能给它增加一种能力；
5. 它拥有可验证的数字主体身份，但不会冒充真人；
6. 我能明确授权它代表我完成一件事，也能拒绝或撤销；
7. 它能完成任务、产生产物，并留下可核验记录；
8. 它能表达并记录一次价值交换，即使结算暂时为模拟或人工；
9. 结果能够回到我的 Digital Me，并由我决定是否成为新的记忆、经验或能力；
10. 应用重启、任务失败或授权失效后，数据和边界仍然可信。

只要这十项成立，MVP 就已经存在。此后再从真实使用中发展 DID、支付、协作网络、能力市场和云边平台，而不是让这些远景阻塞第一个 Digital Me 的诞生。
