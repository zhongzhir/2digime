Digital Me 产品规划 v0.x
主体模型（Subject Model）与认知运行算法

版本：v0.1.1
状态：owner_accepted / active_product_principle
定位：Digital Me 核心产品与技术原则

修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | — | 初稿：主体模型、三层认知、情境驱动、CRT 定位 |
| v0.1.1 | 2026-07-27 | 正式写入「主体连续性与未来开放原则」；固化事实边界与 AI 扩展能力的产品边界 |
| v0.1.1（状态） | 2026-07-27 | Owner 确认该原则写入产品规则；状态由 `design_draft` 升为 `owner_accepted` / `active_product_principle`（版本号仍为 v0.1.1） |

一、背景：从数字助手到数字主体

当前 AI 系统主要解决：

信息获取
内容生成
工具调用
任务执行

但这些系统本质仍然是：

通用 AI 能力 + 用户即时输入

缺少对“人”的长期理解。

Digital Me 的核心目标不是创建一个替代人的 AI，而是建立：

一个属于个人、持续进化、能够表达个人认知与行动方式的数字主体系统。

因此 Digital Me 的核心问题不是：

“如何让 AI 更聪明？”

而是：

“如何让 AI 更准确地理解一个人的主体性，并在长期互动中不断逼近这个人的认知和行动规律？”

二、核心判断：主体不是静态人格，而是动态认知系统

Digital Me 不采用传统人格建模方式。

不试图一次性回答：

“这个人是什么类型？”

因为真实的人：

会学习
会变化
会在不同环境采取不同策略
会随着经历改变价值排序

因此 Digital Me 采用：

Progressive Subject Modeling

渐进式主体建模。

主体模型通过：

任务交互
    ↓
行为观察
    ↓
反馈确认
    ↓
认知抽象
    ↓
主体模型更新
    ↓
下一轮任务调用

不断逼近真实的人。

三、Subject Model 主体模型

Digital Me 建立的不是人格标签，而是：

在不同情境下，一个人如何理解、判断和行动的模型。

主体模型包括：

Subject Model

├── Identity
│   身份、背景、经历基础
│
├── Values
│   价值排序
│
├── Beliefs
│   核心认知与观点
│
├── Preferences
│   稳定偏好
│
├── Experience
│   历史经验与案例
│
├── Capability
│   能力边界
│
├── Decision Pattern
│   判断和决策规律
│
└── Goals
    长期目标与方向

以上不是固定字段。

随着系统运行，可以增加、合并或调整。

四、三层认知结构

Digital Me 必须严格区分三类信息。

1. Reality Layer（事实层）

描述：

已确认发生的事实。

例如：

教育经历
创业经历
产品数据
投资案例
已完成事项

特点：

可验证
可追溯
可审计
2. Cognition Layer（认知层）

描述：

用户如何理解世界。

例如：

投资判断框架
对行业趋势的理解
价值排序
偏好方式

特点：

不是客观事实，但属于人的主体表达。

3. Exploration Layer（探索层）

描述：

对未来可能性的推演。

例如：

商业模式探索
战略假设
科学问题讨论

特点：

允许 AI 发散。

但不能伪装为用户观点。

五、主体连续性与未来开放原则

核心原则（不得弱化）：

Digital Me 的事实边界保护「我是谁、我做过什么、我确认什么」，
但不限制 AI 帮助我推理未知、构造假设和发现新的可能性。

1. 主体资产用于维护

主体资产的用途是维护：

- 主体连续性
- 所有权
- 表达真实性
- 行动可追溯性

它回答的是：这份表达与行动，是否可回溯到「我」、是否仍由我拥有、是否忠实于已确认的我。

2. 主体资产不得用于

主体资产不得被用来：

- 将人冻结为历史数据集合
- 用既有 Memory 限制未来判断
- 要求 AI 字字有来历、句句有出处
- 禁止科学研究、商业探索、战略推演和创意生成中的新可能性

事实边界保护的是主体真实性，不是禁锢探索能力。

3. 正式区分四类主张

在三层认知结构之上，生成与学习必须正式区分：

| 类型 | 含义 | 可否代表 Owner |
|------|------|----------------|
| 已确认主体事实 | 可验证、可追溯的已确认事实 | 可以，须有明确证据 |
| 已形成主体判断 | Owner 已确认或已形成的取舍/立场 | 可以，须有判断资产依据 |
| AI 分析推断 | 基于现有材料作出的分析与推理 | 不可以冒充 Owner 既有结论 |
| 面向未来的开放假设 | 方案、情景、技术路线、商业模式等待验证推演 | 不可以写成已发生事实或已确认立场 |

没有来源的内容，不得升格为「已确认主体事实」或「已形成主体判断」；但可以作为 AI 分析推断或开放假设存在。

4. 角色分工（固化表达）

过去的我 → 提供连续性  
当前的我 → 保有决定权  
AI        → 扩展未来可能性  

Digital Me 不是用过去的 Memory 代替今天的决定，也不是用今天的谨慎消灭明天的可能。

5. 工程规则（固化）

无事实依据，不等于禁止生成。

无事实依据的内容：

- 不得表述为已确认事实；
- 不得表述为 Owner 已有判断；
- 可以表述为 AI 推断、建议、创意或待验证假设。

不要实现「每句话必须有出处」。出处要求服务于高风险事实型断言与主体归属，不服务于扼杀分析、假设与创意。

6. 必须保留 AI 扩展能力的情境

以下情境必须保留 AI 推理未知、构造假设、发现新可能性的能力：

- exploration（开放探索）
- decision_support（辅助决策）
- creation（创作生成）
- 科学研究
- 商业模式探索
- 战略推演
- 未知问题分析

在这些情境中，既有主体事实与判断只作为锚点与边界，不得变成「只能复述 Memory」的硬限制。

7. representation 模式下的扩展边界

representation（代表表达）也不是完全禁止 AI 扩展。

允许：

- 提出建议
- 进行分析
- 给出待验证方案

禁止：

- 把上述内容包装为已经发生的事实
- 把上述内容包装为 Owner 已确认立场

例如：可以写「可考虑……作为未来技术路线」；不得写「已具备……能力 / 已拥有……用户」而无依据。

8. 与 CRT / Claim Posture 的关系

本原则是 Claim Posture、Evidence Boundary、Context Classification 与生成验收的上位产品原则。

工程实现（含 CRT Subject Context Engine）必须服从本原则：  
保护主体连续性与真实性，同时保留面向未来的开放推理与创意能力。

六、情境驱动主体理解

人的行为不是：

人格 → 行动

而是：

主体 + 情境 → 行动

同一个人：

创业时：

高风险偏好

投资时：

谨慎分析

家庭事务：

保守决策

因此 Digital Me 不建立：

用户 = 冒险型

而建立：

Context:
创业投资

Decision Pattern:
愿意承担早期技术风险

Confidence:
High

七、核心算法原则
原则1：少变量，高表达力

避免：

大量人格标签
复杂评分体系
不可维护参数

采用：

少量核心主体变量
+
丰富经验数据
+
AI上下文推理
原则2：事实、判断、探索必须隔离

所有生成内容需要知道：

来源：

Subject Fact

Subject Judgment

AI Exploration

避免：

AI 推测成为用户观点。

本原则与「五、主体连续性与未来开放原则」一致：隔离是为了防止冒充，不是为了禁止推断与假设。

原则3：持续学习，而不是一次训练

主体模型通过：

任务
 ↓
成果
 ↓
用户反馈
 ↓
抽象沉淀
 ↓
主体模型更新

不断进化。

八、CRT 的定位

Digital Me Cognitive Runtime（CRT）不是普通 Memory 系统。

它是：

管理主体资产、上下文装配、认知更新和持续进化的运行层。

核心职责：

判断当前任务需要什么样的主体信息。
装配最相关上下文。
控制事实、判断和探索边界。
根据反馈更新主体模型。

CRT 在执行上述职责时，必须同时遵守「主体连续性与未来开放原则」：  
事实边界保护「我是谁、我做过什么、我确认什么」；不把无出处一律等同于禁止生成。

九、阶段目标

当前阶段不追求完整的人类认知模拟。

目标：

用最少变量，实现高可信的人类主体表达，并建立持续进化闭环。

同时保证：主体连续性不被削弱，面向未来的 AI 扩展能力不被事实边界误伤。